# 独立文件数据库用于本地开发，不连接或改动现有 MySQL 数据。
spring:
  datasource:
    url: jdbc:h2:file:./data/warmpaw;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password: ${PAW_LOCAL_DB_PASSWORD:}
  sql:
    init:
      mode: always
app:
  mock-providers: true
