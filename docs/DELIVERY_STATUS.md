# 本轮交付验收记录

日期：2026-09-14。**状态：进行中，尚未达到可交付验收。**

## 已完成并验证

- 完整 Compose 编排及 Java、Vue/Nginx、Python Dockerfile 已提供，配置语法校验通过。
- 新项目 `warmpaw` 的 MySQL、Redis、Nacos 已实际启动，三个容器均 healthy。
- 新 MySQL 空数据卷已执行项目 schema.sql，未修改原 MySQL 容器或原业务数据。
- Java 全量 44 项测试通过（隔离 H2）；Python 66 项测试通过；前端 2 项 SSE 测试通过。
- 前端类型检查与买家/后台构建通过，后台存在现有大包体积提示。
- 使用现有 DeepSeek 密钥执行一次最小真实请求，HTTP 200，返回“连接成功”。
- 私有配置写入根目录 `.env`，权限 600；未输出密钥。

## 尚未完成

- 应用镜像构建：Docker Hub 认证服务直连超时，尚未进入构建阶段。
  本机 `127.0.0.1:7897` 代理可连接该服务，等待 Docker 代理设置确认。
- 全栈容器运行与浏览器验收：尚未执行，不以宿主机测试代替。
- 原 MySQL、上传文件、AI 数据迁移：等待用户选择迁移现有数据或使用独立演示数据。
- 短信：环境存在阿里云凭据，未验证真实送达；缺少预约短信签名与四项模板，以及交付确认模板。
- OSS：现有文件服务只使用磁盘；需要确认 Bucket/地域及存储选择，再实现和验证。
- 微信授权：已有代码，未发现账号与 HTTPS 回调配置；纯 localhost 无法完成微信登记域名回调。
- 备份命令已提供，完整备份/恢复演练尚未执行。
- AI 经网关与前端的完整会话、知识检索与索引持久化尚未在容器环境验收。

## 本轮范围与数据保护

原工作区存在未提交功能更新，已保留。本轮新增部署文件，未重构原业务模块。
原有 MySQL、Redis、Nginx 和 Nacos 未被接管或删除；新环境默认使用 localhost:8090。
容器外部服务固定真实模式，缺配置不会自动回退为模拟成功。

Java/Python 首次受沙箱端口权限影响；允许临时端口后全量重跑通过。
Python 最初从错误工作目录运行导致导入失败，改在 ai-service 目录运行后已通过。
