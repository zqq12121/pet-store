# 宠物门店电商网站接口文档

> 接口版本：v1 · 文档版本：0.9（开发设计稿）  
> 日期：2026-09-06  
> 依据：[《宠物门店电商网站需求文档（PRD）》v1.2](/Users/zhangquan/Downloads/宠物门店电商网站需求文档.md)  
> 用途：前后端接口约定、数据库设计输入、联调与验收。本文是接口设计，尚未实现或调用真实服务。

## 1. 范围与设计决策

### 1.1 一期范围

单店自营、猫狗活体、一个订单购买一只宠物、到店自提、微信支付、移动端 H5、店主管理后台、DeepSeek + RAG 客服。不设计物流地址、运费、多商家、SKU 数量、购物车、优惠券、支付宝、市场行情问答。

本文完整定义 P0 接口，P1 与二期功能放在第 12 章。PRD 第 8 章提到评价系统，但第 10 章明确一期裁剪，本文按裁剪清单后置。接口使用 REST + JSON，AI 输出使用 SSE；可由 Spring Boot 实现，但契约不绑定具体框架。

### 1.2 设计假设与原需求冲突

以下是设计建议，不表示 PRD 已经确认了这些细节。标记“待确认”的规则可以用于原型和 Mock，上线前须确认。

| 项目 | 本文设计 | 依据/状态 |
|---|---|---|
| 订单单位 | 一订单一宠物，数量固定 1，无购物车 | PRD 1.2、4.1.4、10 |
| 金额 | CNY，整数“分”；例如 680000 表示 ¥6800.00 | 技术约定 |
| 待付款时限 | 创建订单后 30 分钟 | PRD 暂定；快照到订单 |
| 自提时限 | 支付成功后 72 小时，按支付平台确认的支付时间计算 | PRD“3 天”的明确化，待确认 |
| 健康保障期 | 买家现场确认起 168 小时；边界时刻仍可提交 | PRD“7 天”的明确化，待确认 |
| 交付与完成 | 现场确认 + 店主交付后直接 `completed`；不单设 `picked_up` 状态，保留 `pickedUpAt` | 统一 PRD 4.5 流程与第 6 章枚举，待确认 |
| 售后状态 | 单独的 AfterSale 资源；申请/驳回不会直接改为退款成功 | 补足 PRD“已换购/驳回”等未定义枚举 |
| 售后驳回 | 已交付宠物保持 `sold`，不得重新上架 | 修正 PRD 4.5 第 3 条冲突，待确认 |
| 已交付退回 | 实物退回后先 `off`；原售后退款成功或换宠交付完成，并经健康复核后才允许人工上架 | 修正自动重新上架风险，待确认 |
| 未交付退款 | 微信确认退款成功后可释放；仍须校验宠物可售和检疫条件，不合格转 `off` | 对 PRD 4.5 的实施约束 |
| 主动退款 | 待自提时可申请全额退款，由店主审核；自提超期系统自动发起 | 退款申请细则补足，待确认 |
| 退换 | 在线记录审核、退回和换宠交付；一期仅支持同价换宠，差价转人工协商 | PRD有退换但无差价规则；待确认 |
| 治疗费分担 | 以原订单内部分原路退款执行，不超过剩余可退金额 | PRD 4.1.7 的实施建议，待确认 |
| 现场确认方式 | 一期用短信验证码确认，电子手写签名后置 | PRD 允许二选一；设计默认 |
| 管理员登录 | 预置店主账号 + 密码 + 图片验证码，无公开管理员注册 | PRD 未定义账号初始化；设计默认 |
| 法律条款 | 协议正文和适用期限由经营者确认后发布，接口只保存版本和证据 | PRD 4.4；本文不拟定最终法律条款 |

涉及 7 天保障期、疾病责任等争议，系统记录申请与证据并交店主处理；不得让 AI 自行作出责任认定。保障期外保留联系店主入口，接口不据此宣称排除其他依法应承担的责任。

### 1.3 阅读顺序

1. 第 2～3 章：统一约定、状态机，前后端先共同确认。
2. 第 4～9 章：登录、展示、订单、支付、自提售后、后台运营。
3. 第 10～11 章：AI 客服与知识库。
4. 第 12～14 章：后置范围、需求覆盖、联调验收及开发顺序。

## 2. 全局接口约定

### 2.1 地址、鉴权与权限

| 项目 | 约定 |
|---|---|
| 开发地址示例 | `http://localhost:8080`，仅示例，未启动服务 |
| 线上地址 | `https://<业务域名>`，待配置 |
| C 端前缀 | `/api/v1` |
| 后台前缀 | `/api/v1/admin` |
| 微信回调前缀 | `/api/v1/callbacks/wechat-pay` |
| 普通 JSON | `Content-Type: application/json; charset=utf-8` |
| 用户 U | `Authorization: Bearer <userAccessToken>`，仅操作本人资源 |
| 店主 A | `Authorization: Bearer <adminAccessToken>`，后台鉴别 `admin` 角色 |
| 游客 G | `X-Guest-Token: <guestToken>`，只用于游客 AI 会话和行为采集 |
| 公开 P | 无需登录，传入失效 Bearer 时返回 401，不静默降级身份 |
| 微信 W | 微信支付签名验证，不接受用户 Token 替代回调验签 |

Token 是不透明凭证，前端不依赖 JWT 结构。设计默认：用户有效期 2 小时，管理员 30 分钟，到期重新登录；退出立即撤销当前 Token。无刷新接口；后续如增加刷新机制需另行版本说明。禁止把 Token、自提码、短信验证码放 URL、埋点或普通日志中。

所有按 ID 访问的订单、售后、附件、会话均做资源归属校验；不存在和属于他人的资源统一返回 404。前端不传 `userId` 决定操作对象，后端从当前登录身份获取。后台权限与买家权限分离，不能用买家 Token 调用后台接口。

### 2.2 类型和校验规则

| 类型/约定 | 定义 |
|---|---|
| `id` | JSON string，长度 1～64；数据库 BIGINT 也转字符串，避免 JS 精度丢失 |
| `datetime` | ISO 8601，带时区，如 `2026-09-06T21:00:00+08:00`；服务端保存统一时间，营业日按 Asia/Shanghai |
| `date` | `YYYY-MM-DD` |
| `amount` | 非负整数分；`priceAmount` 必须 > 0；单笔上限设计为 100000000 分 |
| `phone` | 一期中国大陆手机号 string，`^1[3-9][0-9]{9}$`；不公开买家完整手机号；门店联系电话按经营设置公开 |
| `version` | 正整数；可编辑资源的乐观锁版本，更新成功加 1，旧版本返回 409 |
| 字段命名 | camelCase；枚举采用小写 snake_case |
| `!` | 字段必填且非 null；`?` 表示请求可省略；响应中 `T\|null` 明确允许空值 |
| 可选字段 | PATCH 缺省表示不修改，null 仅在字段允许清空时有效；PUT 为完整替换 |
| 默认校验 | 字符串去除两端空白；未知请求字段拒绝为 400，禁止客户端提交当前接口未声明为可写的状态、金额、归属等字段 |

长度、文件大小、限流阈值均为开发默认值，并非 PRD 已确认指标。文中 `A + B` 表示对象字段合并；`Page<T>` 表示标准分页；空集合返回 `[]`，不返回 null。

### 2.3 统一响应

普通成功返回 HTTP 200；创建资源返回 201；异步受理返回 202。均使用同一包装：

```json
{
  "code": "OK",
  "message": "success",
  "data": { "orderId": "20001" },
  "requestId": "req_demo",
  "serverTime": "2026-09-06T21:00:00+08:00"
}
```

无返回对象的操作返回 `data: null`。本文各接口“返回”描述的是 `data`，不重复外层包装。SSE、图片/模板下载及微信支付回调使用各自协议，详见对应章节。

```json
{
  "code": "VALIDATION_ERROR",
  "message": "联系人手机号格式不正确",
  "data": null,
  "errors": [{ "field": "contactPhone", "reason": "INVALID_PHONE" }],
  "requestId": "req_demo",
  "serverTime": "2026-09-06T21:00:00+08:00"
}
```

`errors` 只在字段校验失败时出现。分页参数统一为 `page?: integer=1, >=1`、`pageSize?: integer=20, 1..100`；返回：

```json
{ "items": [], "page": 1, "pageSize": 20, "total": 0 }
```

### 2.4 通用错误码

| HTTP | code | 前端处理 |
|---|---|---|
| 400 | VALIDATION_ERROR / INVALID_ARGUMENT | 展示字段错误 |
| 400 | CAPTCHA_INVALID / SMS_CODE_INVALID / SMS_CODE_EXPIRED | 重新获取验证码 |
| 401 | UNAUTHORIZED / TOKEN_EXPIRED | 登录，保留不含敏感信息的返回页面 |
| 403 | FORBIDDEN | 无角色权限 |
| 404 | RESOURCE_NOT_FOUND | 资源不存在或不可访问 |
| 409 | VERSION_CONFLICT | 刷新资源后重新编辑 |
| 409 | IDEMPOTENCY_CONFLICT | 相同幂等键的内容不一致 |
| 409 | PET_NOT_AVAILABLE | 宠物已被预订、已售或下架，刷新详情 |
| 409 | PRICE_CHANGED / AGREEMENT_CHANGED | 重新预览结算/阅读新协议 |
| 409 | ORDER_STATE_CONFLICT / ORDER_EXPIRED | 刷新订单状态 |
| 409 | PAYMENT_IN_PROGRESS / REFUND_IN_PROGRESS | 查询订单或退款状态，不新建重复请求 |
| 409 | AFTER_SALE_EXISTS / RETURN_REQUIRED | 查看已有售后/等待到店退回 |
| 409 | WECHAT_ALREADY_BOUND | 联系店主处理身份冲突，不静默转移绑定 |
| 422 | QUARANTINE_REQUIRED / FILE_PURPOSE_MISMATCH | 补充证明或正确用途附件 |
| 422 | AFTER_SALE_WINDOW_EXPIRED / REFUND_AMOUNT_EXCEEDED | 展示服务端说明及联系店主入口 |
| 429 | RATE_LIMITED | 按 `Retry-After` 秒数等待 |
| 502 | UPSTREAM_ERROR | 第三方失败，按业务资源状态重试 |
| 503 | SERVICE_UNAVAILABLE | 服务暂不可用，保留表单；交易结果以查询为准 |

专用业务错误在章节中补充。HTTP 4xx/5xx 不能包装成 HTTP 200。错误信息不得包含第三方密钥、SQL、堆栈或其他用户资料。

### 2.5 幂等、并发和截止时间

所有创建订单、取消订单、发起支付、提交售后、审核、退款重试、买家核验、交付、退回验收、换宠锁定/交付请求必须携带 `Idempotency-Key: <UUID>`。作用域为“当前身份 + HTTP 方法 + 路径 + key”。首次请求保存请求体摘要及资源 ID；同 key 同内容返回原资源的当前状态，同 key 不同内容返回 409。业务结果至少保留 7 天；订单支付单号、退款单号、自提交付和有效售后另有数据库唯一约束，超过缓存期限也不得重复执行。

提交后网络超时，客户端用原 key 重试或查询资源，不生成新 key。文件上传、短信发送也做频率限制，但不复用交易幂等结果。管理员更新提交 `version`；状态迁移使用数据库事务和条件更新。仅靠 Redis 锁或 key 过期不能释放宠物。

`expiresAt`、`pickupDeadlineAt`、`healthGuaranteeExpiresAt`、协议/门店/价格快照均由后端生成。配置改变仅影响新订单；服务器时间决定截止条件，前端倒计时仅用于展示。

## 3. 状态机与核心业务规则

### 3.1 宠物状态

| status | 含义 | 是否允许下单 |
|---|---|---|
| off | 未上架/待复检/已退回待处理 | 否 |
| on_sale | 在售且符合上架条件 | 是 |
| reserved | 被待付款/待自提订单或换宠流程占用 | 否 |
| sold | 已交付买家；售后申请和驳回期间仍保持此状态 | 否 |

新建宠物默认 `off`。店主只有“上架/下架”操作，不能手填 `reserved/sold`。商品占用使用 `activeOrderId` 或等效唯一约束；下单必须在同一 MySQL 事务中执行条件占用并插入订单，失败整体回滚。Redis 只用于加速和辅助限流，MySQL 是事实来源。

`reserved` 商品不得改售价、个体信息、检疫证明或强制上下架；`sold` 的原始档案和订单快照不可覆盖。售后退回后，按退回记录复检并通过状态门槛才可重新上架。

### 3.2 订单与支付状态

| order.status | 进入条件 | 允许的主要下一步 |
|---|---|---|
| pending_paid | 下单成功、宠物已锁定 | 发起支付；取消；到期关单 |
| closing | 用户取消或超时，正在核实支付/关闭平台交易 | 未支付且关闭成功→cancelled；已支付→paid 或 refunding |
| cancelled | 未支付且本地无支付单/平台确认已关闭 | 终态；按可售条件释放宠物 |
| paid | 已验证的微信支付成功，待自提 | 现场核验并交付→completed；退款获批/超期→refunding |
| completed | 买家现场确认且店主完成交付 | 售后申请；退款执行时可进入 refunding |
| refunding | 全额或部分退款已受理，平台尚未确认结果 | 全额成功→refunded；部分成功→退款前的 paid/completed |
| refunded | 累计成功退款等于实际支付金额 | 财务退款终态；实物库存依退回/交付事实处理 |

`closing` 为本设计新增的安全过渡状态。`pickedUpAt` 是事件时间，不另设订单状态 `picked_up`。售后申请、审核和换宠结果由 `afterSale` 表示，不能用一个订单状态同时表达所有含义。

支付对象 `Payment.status = not_created | pending | succeeded | closed | failed`；退款对象 `Refund.status = processing | succeeded | failed`。退款失败保留 `refunding` 并显示 `refund.status=failed`，店主核实后重试，不释放库存。数据库事务内写入支付/退款意图，外部调用通过可靠任务重试和查单对账恢复，不能假定本地事务会回滚微信操作。

### 3.3 支付与库存竞态

1. 待付款取消/到期：禁止创建新的预支付，进入 `closing`；无支付单则本地取消，否则先微信查单/关单。仅“确认未支付且已关闭”才能释放宠物。
2. 关单时已支付：以经验证的支付事实记账。用户取消请求已成立或支付时间晚于付款截止时，走全额退款；否则进入待自提。退款期间仍占用宠物。
3. 异常迟到的成功回调与 `cancelled` 冲突：禁止覆盖其他新订单的占用，登记异常并发起原路退款/人工待办，不重新锁给旧订单。
4. 自提超期：默认 `now > pickupDeadlineAt` 才超期，任务和交付操作竞争同一订单版本；成功抢占退款后即禁止交付。超期只代表发起退款，退款成功前不释放。
5. 已交付宠物：申请/驳回售后不释放；实际退回→`off`；原售后退款成功或换宠交付完成，且复检通过后可人工上架。健康问题宠物不能因财务退款自动变为在售。
6. 换宠：替换宠物也使用同一占用机制。已审核但尚未退回不锁替换宠物；锁定后直到交付/取消换宠不得被其他订单购买。

### 3.4 售后状态

`none` 仅用于订单汇总，实际 AfterSale 枚举为：

| status | 含义 |
|---|---|
| pending_review | 等待店主审核 |
| rejected | 已驳回，记录原因；P0 保留联系店主，补充申诉属 P1 |
| awaiting_return | 全额退款/换宠获批，待实物退回 |
| refunding | 退款处理中（失败时附退款失败原因） |
| awaiting_exchange | 已退回，待安排并交付替换宠物 |
| resolved | 已完成退款/治疗费分担/换宠 |

同订单同时最多一个未结束售后；`rejected/resolved` 为已结束。被拒申请的补材料再申诉属于 P1，P0 返回 `AFTER_SALE_REAPPLY_NOT_SUPPORTED`（409）并引导联系店主；已经 resolved 的申请不阻止保障期内新的健康问题申请，新申请仍须检查订单/实物状态、期限和可退余额。自动超期退款独立于申诉限制。部分退款不等于订单已全额退款，剩余可退金额为实际支付额减累计成功和处理中退款额。

## 4. 账号、登录与用户资料

### 4.1 GET /api/v1/auth/captchas

- 权限：P。Query：`purpose!: sms | admin_login`。
- 返回：`{captchaId:id, imageBase64:string, expiresIn:integer}`；图片为 data URL，默认 120 秒有效，一次性使用。
- `imageBase64` 仅为图片，不返回验证码明文；IP 默认 30 次/分钟。

### 4.2 POST /api/v1/auth/sms-codes

- 权限：P；登录/微信绑定共用。Body：`phone!:phone, purpose!:login|wechat_bind, captchaId!:id, captchaCode!:string(4..6)`；`wechat_bind` 另需 `bindTicket!:string`。
- 返回：`{smsRequestId:id, expiresIn:300, retryAfter:60}`，HTTP 200，不返回短信内容。
- 默认同手机号 60 秒 1 条、每日 10 条、同 IP 每日 50 条；平台受理失败返回 502。短信六位数字、绑定手机号/用途/请求 ID，最多校验 5 次，成功即消费。
- 本接口不发送自提确认短信，自提短信由 8.1 的专用接口发送。

### 4.3 POST /api/v1/auth/sms-login

- 权限：P。Body：`phone!:phone, smsRequestId!:id, smsCode!:string(6位数字)`。
- 返回 `LoginResult`；无账号则在唯一手机号约束下自动注册，同一手机号不会创建重复账户。

```json
{
  "phone": "13800138000",
  "smsRequestId": "sms_10001",
  "smsCode": "123456"
}
```

`LoginResult`：

```json
{
  "accessToken": "<opaque-token>",
  "tokenType": "Bearer",
  "expiresIn": 7200,
  "user": { "id": "10001", "nickname": "新用户", "avatarUrl": null, "phoneMasked": "138****8000" }
}
```

示例验证码仅用于说明字段，不是开发环境万能码。`UserProfile={id:id,nickname:string,avatarUrl:string|null,phoneMasked:string}`。

### 4.4 GET /api/v1/auth/wechat/authorize-url

- 权限：P。Query：`scene!:login|pay, returnPath!:string(1..500)`。
- `returnPath` 只接受本站允许路径，以 `/` 开头且不得为 `//`；禁止任意外部 redirect。
- 返回：`{authorizeUrl:string, expiresIn:300}`。后端生成一次性随机 state，绑定发起浏览器的 HttpOnly、Secure、SameSite=Lax 临时 Cookie。微信 redirect_uri 为后端配置的固定前端回调页面。
- `scene=pay` 必须携带 U Token，获取用于 JSAPI 的同 appId 微信身份；`scene=login` 可游客使用。微信授权账号主体、回调域名和支付 appId 绑定上线前验证。

### 4.5 POST /api/v1/auth/wechat/login

- 权限：P；`scene=pay` 时要求 U，且必须是发起 state 的同一用户。Body：`code!:string, state!:string`。
- 校验一次性 state + Cookie、code 有效期，后端换取 openid；不接受客户端自行提交 openid。
- 返回二选一：`{status:"authenticated",login:LoginResult}`，或 `{status:"bind_required",bindTicket:string,expiresIn:300}`。
- `scene=pay` 成功返回 `{status:"wechat_ready"}`，不切换当前账号。若微信身份已绑定另一用户返回 `WECHAT_ALREADY_BOUND`。
- code/state 无效返回 400 `WECHAT_AUTH_INVALID`。不将 openid、微信 access_token 返回前端。

### 4.6 POST /api/v1/auth/wechat/bind-phone

- 权限：持有效一次性 bindTicket；Body：`bindTicket!:string, phone!:phone, smsRequestId!:id, smsCode!:string(6位)`，必须验证 `wechat_bind` 用途短信。
- 返回 `LoginResult`。手机号已存在时绑定到该账号，不再新建；手机号或微信已绑定不同身份时返回 409，不自动覆盖。
- 用户主键和手机号唯一，微信身份以 `(appId, openid)` 唯一，不能只按昵称/头像合并。

### 4.7 POST /api/v1/auth/logout

- 权限：U。无 Body；返回 null。撤销当前 Token，重复退出返回成功；前端清除本地会话。

### 4.8 GET /api/v1/me

- 权限：U。返回 `UserProfile + {createdAt:datetime}`。

### 4.9 PATCH /api/v1/me

- 权限：U。Body：`nickname?:string(1..30), avatarFileId?:id|null`，至少提交一个字段。
- 返回 `UserProfile`；头像需当前用户上传、用途为 `avatar`。手机号不通过本接口修改。

### 4.10 POST /api/v1/admin/auth/login

- 权限：P。Body：`username!:string(3..32),password!:string(8..128),captchaId!:id,captchaCode!:string`，验证码用途 `admin_login`。
- 返回 `{accessToken:string,tokenType:"Bearer",expiresIn:1800,admin:{id:id,username:string,role:"admin"}}`。
- 默认同账号 10 分钟内连续失败 5 次临时限流；密码采用单向哈希存储，不在接口中下发预置密码。

### 4.11 POST /api/v1/admin/auth/logout

- 权限：A。无 Body，返回 null，立即撤销当前 Token。

## 5. 首页、商品、门店与协议

### 5.1 公共 DTO

`Shop`：

| 字段 | 类型 | 说明 |
|---|---|---|
| id / name | id / string | 单店固定 ID；名称 1..80 |
| address | string | 地址 1..200 |
| latitude / longitude | number | 纬度 -90..90，经度 -180..180 |
| coordinateSystem | string | 固定 `gcj02`，供地图导航，不能混用坐标系 |
| phone / wechat | string / string | 对外联系电话、微信号 |
| businessHours | string | 展示文本 1..200，如“周一至周日 10:00—20:00” |
| pickupInstructions | string | 到店自提说明 1..1000 |

`ProductCard`：`{id:id,name:string,category:cat|dog,breed:string,coverUrl:string,priceAmount:amount,currency:"CNY",gender:male|female,ageMonths:integer,status:off|on_sale|reserved|sold,isRecommended:boolean,publishedAt:datetime|null}`。

`ProductDetail = ProductCard +`：

| 字段 | 类型 | 说明 |
|---|---|---|
| version | integer | 商品版本，结算检查用 |
| weightKg | number | >0、最多两位小数 |
| color / personalityTags | string / string[] | 毛色、性格关键词 |
| vaccineStatus / dewormStatus | string / string | 真实记录展示文本，分别≤500字符；不推断“已免疫” |
| description / feedingNotes / healthDescription | string | 介绍、饲养要点、健康信息，分别≤10000字符 |
| images | array | `{fileId:id,url:string,sortOrder:integer}`，1..9 张 |
| videoUrl | string\|null | 0..1 个视频 |
| quarantine | object | `{certificateNo:string,images:[{fileId:id,url:string}],validUntil:date\|null,reviewStatus:valid\|pending\|expired}` |
| shop | Shop | 门店及导航数据 |
| purchaseAllowed | boolean | 后端结合状态、价格、证明及经营配置计算 |
| purchaseBlockedReason | string\|null | 不可购买原因 |

年龄使用月龄非自由文本；管理员维护 `ageMonths:0..360`，可选 `birthDate:date|null`。有出生日期时后端按日期计算月龄，无出生日期按登记月龄展示。检疫证明对外展示的图片须为店主确认可公开的副本，原始材料不作为公共链接。

### 5.2 GET /api/v1/shop

- 权限：P。无 Query，返回 `Shop`。

### 5.3 GET /api/v1/home

- 权限：P。无 Query，返回 `{banners:Banner[],categories:Category[],recommendedPets:ProductCard[],latestPets:ProductCard[],shop:Shop}`。
- `Banner={id:id,title:string,imageUrl:string,linkType:pet|notice|none,petId:id|null,noticeText:string|null,sortOrder:integer}`。
- `Category={code:cat|dog,name:string,breeds:string[]}`；手动推荐优先按推荐排序，再按上架时间倒序；最新商品按上架时间倒序，各最多 8 个。
- Banner 的活动仅是展示，不包含优惠计算。Banner 不接受任意外部跳转。
- 仅返回 enabled=true 的 Banner 和 on_sale/reserved 商品，不输出 off 草稿或 sold 商品推荐；跳往不可公开商品的 Banner 自动过滤。

### 5.4 GET /api/v1/pet-categories

- 权限：P。无 Query，返回 `Category[]`。品种列表由初始猫狗字典和已维护的商品品种去重形成；一期不新增字典管理系统。

### 5.5 GET /api/v1/pets

- 权限：P。返回 `Page<ProductCard>`。

| Query 字段 | 类型/默认 | 校验/含义 |
|---|---|---|
| keyword | string，可选 | 1..50，匹配品种、毛色、性格和名称 |
| category / breed | cat\|dog / string | 可选；品种≤50字符，与分类一致 |
| minPriceAmount / maxPriceAmount | amount | 可选；min≤max |
| gender | male\|female | 可选 |
| minAgeMonths / maxAgeMonths | integer | 可选，0..360，min≤max |
| color | string | 可选，≤30字符 |
| status | on_sale\|reserved | 可选；默认同时展示两者 |
| sort | comprehensive\|price_asc\|price_desc\|newest | 默认 comprehensive |
| page / pageSize | integer | 见 2.3 |

综合排序按推荐优先、推荐排序、上架时间倒序、ID 倒序；价格排序同价时按 ID 倒序，保证稳定分页。不提供随机推荐算法。用户输入排序字段不能直接拼入 SQL。

```http
GET /api/v1/pets?category=cat&minPriceAmount=300000&maxPriceAmount=800000&sort=price_asc&page=1&pageSize=20
```

### 5.6 GET /api/v1/pets/{petId}

- 权限：P。返回 `ProductDetail`。`reserved/sold` 可通过历史链接展示但不可买；`off` 对公开端返回 404。已购买订单始终通过订单快照查看，不依赖商品详情仍公开。

### 5.7 GET /api/v1/agreements/current

- 权限：P。Query：`type!:live_pet_trade|pickup_confirmation`。
- 返回 `{id:id,type:string,version:string,title:string,content:string,contentFormat:"markdown",contentHash:string,publishedAt:datetime,healthGuaranteeDays:integer|null}`。
- 活体交易协议在下单前阅读；自提确认文本在现场确认前展示。前端渲染 Markdown 时过滤原始 HTML 和危险链接。
- 没有已发布版本返回 503 `AGREEMENT_NOT_READY`，禁止相应交易动作。协议正文示例不在本接口文档中代替正式文本。

## 6. 结算与订单

### 6.1 POST /api/v1/orders/preview

- 权限：U。Body：`petId!:id`；只预览，不锁宠物、不创建订单。
- 返回 `{product:ProductCard,productVersion:integer,amount:amount,currency:"CNY",shop:Shop,agreementVersion:string,agreementContentHash:string,paymentTimeoutMinutes:integer,pickupRetentionHours:integer,healthGuaranteeDays:integer,purchaseAllowed:boolean,blockedReason:string|null}`。期限取当前门店配置和当前协议的实际值，默认30分钟/72小时/7天，不是固定字面量类型。
- 无需客户端计算价格；预览成功不保证创建时仍可售。当前协议和上架条件不满足时拒绝继续下单。

### 6.2 POST /api/v1/orders

- 权限：U；需 Idempotency-Key。成功 201，返回 `OrderDetail`。

| Body 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| petId | id | 是 | 唯一个体，无 quantity 参数 |
| productVersion | integer | 是 | 预览返回版本；变化返回 VERSION_CONFLICT |
| expectedAmount | amount | 是 | 用户看到的金额，只做变价校验，不能决定订单金额 |
| contactName | string | 是 | 1..30，不自动信任微信昵称 |
| contactPhone | phone | 是 | 一期要求等于当前已验证登录手机号；代他人提货后置 |
| remark | string | 否 | ≤500，不作为改变价格或交易规则的指令 |
| agreementVersion | string | 是 | 当前发布的活体交易协议版本，≤32 |
| agreementContentHash | string | 是 | 预览/协议接口返回的 SHA-256 |
| agreementAccepted | boolean | 是 | 必须为 true；签署时间由服务端记录 |

```http
POST /api/v1/orders
Authorization: Bearer <userAccessToken>
Idempotency-Key: 7c0a0000-1111-4222-8333-000000000001
Content-Type: application/json
```

```json
{
  "petId": "30001",
  "productVersion": 3,
  "expectedAmount": 680000,
  "contactName": "张女士",
  "contactPhone": "13800138000",
  "remark": "计划周日下午到店",
  "agreementVersion": "live-pet-v1",
  "agreementContentHash": "<接口返回的SHA-256>",
  "agreementAccepted": true
}
```

一个事务内检查商品/证明/协议/价格→条件占用商品→固化快照→创建订单。`PRICE_CHANGED`、`AGREEMENT_CHANGED`、`PET_NOT_AVAILABLE` 都不留下半成品订单或锁。无议价、优惠、运费接口；以后改商品价不改历史订单。

### 6.3 Order DTO

`OrderSummary`：

| 字段 | 类型 | 说明 |
|---|---|---|
| id / orderNo | id / string | 内部 ID、对外订单号，各自唯一 |
| status / statusText | enum / string | 见 3.2；展示名由服务端给出 |
| product | object | `{id:id,name:string,breed:string,coverUrl:string}`，来自下单快照 |
| amount / refundedAmount / currency | amount / amount / string | 应付、累计成功退款、CNY |
| createdAt / expiresAt | datetime / datetime | 创建和付款截止 |
| paidAt / pickupDeadlineAt / completedAt | datetime\|null | 未发生时为 null |
| afterSaleStatus | string | `none` 或 3.4 中枚举 |
| availableActions | string[] | `pay,cancel,view_pickup,confirm_health,apply_after_sale,view_after_sale,contact_shop` 的可用子集 |

`OrderDetail = OrderSummary +`：

| 字段 | 类型 | 说明 |
|---|---|---|
| priceSnapshot | object | `{unitPriceAmount:amount,quantity:1,totalAmount:amount,currency:"CNY"}` |
| productSnapshot | object | 原商品 `name,category,breed,gender,ageMonths,weightKg,color,vaccineStatus,dewormStatus,images,quarantine` 的快照；图片持久关联 |
| shopSnapshot | Shop | 下单时门店信息；实际变更通过店主联系确认，不静默覆盖证据 |
| contactName / contactPhoneMasked / remark | string | 联系人、脱敏电话、备注 |
| agreement | object | `{version:string,contentHash:string,title:string,content:string,signedAt:datetime}`，保存下单时正文 |
| payment | object | `{paymentId:id\|null,status:string,paidAmount:amount,paidAt:datetime\|null}` |
| pickup | object\|null | `{code:string,qrPayload:string,expiresAt:datetime,buyerConfirmedAt:datetime\|null}`，仅 `paid` 且未进入售后阻塞时返回 |
| pickupEvidence | object\|null | `{confirmationId:id,textVersion:string,textHash:string,confirmationText:string,checks:object,confirmedAt:datetime,method:"sms",operatorId:id\|null,deliveredAt:datetime\|null}` |
| healthGuaranteeExpiresAt | datetime\|null | 服务端现场确认时间 + 订单保障期快照 |
| afterSale | AfterSaleSummary\|null | 当前/最近售后，详见 8.6 |
| afterSaleEligibility | object | `{allowedTypes:string[],allowedResolutions:string[],blockedReason:string\|null}`，按当前状态、保障期、余额、换宠开关计算；仅指导界面，提交时重新校验 |
| deliveryQuarantine | object\|null | 实际交付的证明快照，公开字段结构同 ProductDetail.quarantine；与下单证明分开保存，交付前为null |
| refunds | Refund[] | 退款记录，详见 7.5 |
| closedAt / pickedUpAt / cancelledAt | datetime\|null | 各事件时间 |
| cancelReason | string\|null | `user_cancelled\|payment_timeout\|payment_conflict` |

`availableActions` 方便前端渲染，后端每次写操作仍重新校验。核验后自提码不再展示。日志/缓存中的自提码采用受控存储，不把自提码作为公开详情 ID。

### 6.4 GET /api/v1/orders

- 权限：U。Query：`tab?:all|pending_paid|pending_pickup|completed|after_sale=all, page?,pageSize?`。
- 返回 `Page<OrderSummary>`，创建时间倒序。
- `pending_paid` 包括 pending_paid/closing；`pending_pickup` 包括 paid；`completed` 包括 completed；`after_sale` 包括有售后单或退款记录的订单；标签可重叠。

### 6.5 GET /api/v1/orders/{orderId}

- 权限：U 本人。返回 `OrderDetail`；包含已固化的协议与签署记录，不请求当前协议来代替历史版本。

### 6.6 POST /api/v1/orders/{orderId}/cancel

- 权限：U 本人；需幂等键。Body：`reason?:string(≤200)`。
- 仅 pending_paid 可发起；返回 202 和 `{orderId:id,status:3.2订单枚举,nextPollAfter:integer}`。完全本地取消也统一 202；nextPollAfter 在处理中为2、终态为0。幂等重放可返回 refunded 等后续状态，客户端以当前状态展示。
- 客户端不得在受理后立即显示“宠物已释放”。同请求重放返回当前结果；其他已付/完成状态返回 409。

## 7. 微信支付、退款与回调

### 7.1 GET /api/v1/payment-capabilities

- 权限：P。无 Query，返回 `{currency:"CNY",methods:[{scene:jsapi|h5,enabled:boolean,disabledReason:string|null}]}`。
- 微信内选择 JSAPI；外部移动浏览器选择 H5。两者分别依赖公众号授权/商户绑定与 H5 支付开通、域名配置；不能因商户号存在就假定两个场景都已开通。
- 不可用时前端展示原因和联系店主，不生成虚假支付入口。本接口不泄露商户密钥或配置证书。

### 7.2 POST /api/v1/orders/{orderId}/payments

- 权限：U 本人；需幂等键。Body：`scene!:jsapi|h5,returnPath?:string(本站允许路径，≤500)`。
- 校验订单 pending_paid、未过期、无关单任务。价格、商户单号、用户 openid、客户端 IP、notify_url 都由后端确定；不得接受客户端 `amount/openid/notifyUrl`。
- JSAPI 尚未取得当前用户、当前支付 appId 的微信身份时返回 409 `WECHAT_AUTH_REQUIRED`，前端走 4.4 的 scene=pay；H5 未开通返回 422 `PAYMENT_SCENE_UNAVAILABLE`。
- 首次创建 HTTP 201，返回以下按 scene 区分的联合类型。幂等重放 HTTP 200，status 使用完整 Payment.status；仅 pending 且未过期时附 invokeParams/h5Url，其他状态只返回 paymentId、status、scene、expiresAt，不返回可再次拉起的参数。以下为首次创建示例：

```json
{
  "paymentId": "pay_20001",
  "status": "pending",
  "scene": "jsapi",
  "expiresAt": "2026-09-06T21:30:00+08:00",
  "invokeParams": {
    "appId": "<支付绑定的公众号appId>",
    "timeStamp": "<秒时间戳字符串>",
    "nonceStr": "<随机串>",
    "package": "prepay_id=<微信返回值>",
    "signType": "RSA",
    "paySign": "<服务端生成签名>"
  }
}
```

H5 返回 `{paymentId:id,status:"pending",scene:"h5",expiresAt:datetime,h5Url:string}`，不含 invokeParams。H5 跳转返回页面只能触发查单，不携带“已支付”业务权限。

同订单至多一个未关闭的微信商户支付单；不同幂等键重复发起也返回已有同场景参数。切换场景须先确认旧支付单关闭，再建新的商户单号；无法立即确认时返回 409 `PAYMENT_IN_PROGRESS`。预支付到期不得超过订单付款截止。

### 7.3 GET /api/v1/orders/{orderId}/payment

- 权限：U 本人。返回 `{orderId:id,paymentId:id|null,status:not_created|pending|succeeded|closed|failed,paidAmount:amount,paidAt:datetime|null,orderStatus:string,nextPollAfter:integer}`。
- 首次支付结果页访问可触发后端节流查微信订单并对账；建议每 2 秒轮询、最多 30 秒，之后转订单页手动刷新。收到前端 SDK 成功回调不能直接置 paid。
- 对已验证支付成功的订单创建一次性高熵二维码 Token 和 8 位数字备用自提码；数字码只在后台已登录接口查询，连续错误限流。全局有效自提码去重、交付/退款后失效。

### 7.4 POST /api/v1/callbacks/wechat-pay/payments

- 权限：W，不需要 Bearer，公网 HTTPS 地址固定配置到微信商户。
- Header：`Wechatpay-Timestamp, Wechatpay-Nonce, Wechatpay-Serial, Wechatpay-Signature`，按当前商户采用的微信支付公钥/平台证书模式验签。
- Body：微信支付 API v3 原始通知，不使用本系统 DTO；必须先基于原始 HTTP body 验签，校验时间窗口/重放，再解密 resource。
- 校验通知事件、商户、appId、out_trade_no、微信 transaction_id、金额 total 与 payer_total（如适用）、币种和支付成功状态；不匹配不得入账。transaction_id/out_trade_no 建唯一约束。
- 事务持久化成功或已处理的同一合法通知返回 HTTP 200 空 Body；失败返回 HTTP 4xx/5xx：`{"code":"FAIL","message":"通知处理失败"}`。这是微信协议例外，不返回业务包装，也不把详细验签错误暴露给外部。
- 回调丢失时由后端定时查单补偿；重复/乱序回调不能退回旧状态，不能重复发自提码或扣库存。回调具体字段和签名方式以接入时微信支付官方 API v3 为准，本文不替代平台文档。

### 7.5 Refund DTO 与执行原则

`Refund={id:id,refundNo:string,afterSaleId:id|null,reason:customer_request|health_issue|pickup_timeout|payment_conflict|treatment_share,amount:amount,status:processing|succeeded|failed,createdAt:datetime,succeededAt:datetime|null,failureReason:string|null}`。

用户不直接指定任意退款金额调用支付平台。审核通过/自提超期任务/支付冲突触发后端创建 Refund，金额由已付金额和审核结果计算。原路退回微信支付，`refundNo` 是稳定唯一的商户退款单号。API 超时属于结果未知，先按同退款单号查单；不得立刻换退款单号再退一次。

同一订单退款申请的“成功 + 处理中”总额不得大于实际支付额，采用事务串行校验；支持治疗费分担的部分退款。退款结果通过 OrderDetail、AfterSaleDetail 查询，无需暴露平台退款接口给买家。

### 7.6 POST /api/v1/callbacks/wechat-pay/refunds

- 权限：W。签名 Header、原始通知、验证步骤与 7.4 相同。
- 解密后核验商户退款单号、原支付单号、平台退款单号、退款金额/币种以及通知状态；只有明确成功才将 Refund 置 succeeded，计算累计退款并推进订单/售后/库存。
- 平台处理中/异常/关闭不能当作退款成功。成功返回 HTTP 200 空 Body；未持久化或验证失败按 7.4 的 FAIL 响应。后端定时查退款状态对账，不依赖买家页面常开。

### 7.7 POST /api/v1/admin/refunds/{refundId}/retry

- 权限：A；需幂等键。Body：`reason!:string(1..500)`。
- 仅内部确认可重试的失败退款可执行；结果不明或平台仍处理中返回 409。先查原单，再复用原平台支持的退款标识/按平台要求记录后继尝试，业务退款金额只占用一次。
- 返回 202 `Refund`。不得由管理员编辑 amount 来绕过已批准金额。

## 8. 现场自提、售后和换宠

### 8.1 POST /api/v1/orders/{orderId}/pickup-verification-codes

- 权限：U 本人。无 Body；仅 paid、未超期且没有阻塞售后时允许。
- 给订单已验证联系手机号发短信，返回 `{smsRequestId:id,phoneMasked:string,expiresIn:300,retryAfter:60}`。频率限制同 4.2，验证码作用域含 orderId 和用途 pickup_confirm；不能用登录验证码核销。

### 8.2 POST /api/v1/orders/{orderId}/pickup-confirmations

- 权限：U 本人；需幂等键，必须买家自行操作，管理员不能代填 buyerConfirmedAt。
- Body：`smsRequestId!:id,smsCode!:string(6位),confirmationVersion!:string,confirmationContentHash!:string,accepted!:true,checks!:{mentalState:true,eyesAndNose:true,coat:true,excretion:true}`。
- 任一健康项目未确认则不能提交“交付通过”，前端引导现场店主处理；true 表示用户核验确认，不是系统生成健康结论。
- 校验当前现场确认文本、短信与订单绑定、状态和自提期限；服务端冻结文本、版本、hash、确认手机号哈希、短信校验流水、时间及必要审计元数据，不存短信明文。
- 返回 `{confirmationId:id,confirmedAt:datetime,validUntil:datetime,healthGuaranteeExpiresAt:datetime}`。现场确认默认 10 分钟内须交付且不能越过自提截止；过期需重新确认，旧证据留存。健康保障期以实际用于交付的确认时间计算。

### 8.3 POST /api/v1/admin/pickups/lookup

- 权限：A。Body 二选一：`{pickupCode:string(8位数字)}` 或 `{qrPayload:string(≤512)}`；不放 Query 避免 URL 日志泄露。
- 返回 `{order:OrderSummary,contactName:string,contactPhoneMasked:string,buyerConfirmation:{id:id,confirmedAt:datetime,validUntil:datetime}|null,checksPassed:boolean,pickupAllowed:boolean,blockedReason:string|null}`。
- 只查询、不交付。无匹配或已失效返回 404；同管理员连续错误 5 次暂时限流 60 秒。

### 8.4 POST /api/v1/admin/orders/{orderId}/pickup

- 权限：A；需幂等键。Body：`confirmationId!:id,pickupCode?:string,qrPayload?:string,quarantineVerified!:true,deliveryQuarantine?:DeliveryQuarantine`，code/payload 二选一。
- `DeliveryQuarantine={certificateNo:string(1..100),publicImageFileIds:id[](1..5),originalFileIds:id[](0..5),validUntil:date|null}`，附件用途分别 quarantine_public/quarantine_original。证明未变可省略并使用原商品证明；原证明已过期或不适用时必须提交实际有效的交付证明。店主核对证明对应当前唯一个体并确认有效，不能以另一宠物的证明替代。
- 同一事务重新检查有效码、买家确认归属/有效期、订单 paid、未超自提期限、无退款/待审售后、交付证明有效及商品占用一致，再执行订单 completed、商品 sold、交付证明/核验证据写入、码失效。更新交付证明不覆盖原商品和下单证明快照；私有原件不投影到买家DTO。pickupEvidence.checks 保存8.2的四项健康确认记录。
- 返回 `{orderId:id,status:"completed",pickedUpAt:datetime,healthGuaranteeExpiresAt:datetime}`。重复请求不延长保障期或重复交付。
- 冲突返回 409 `PICKUP_CONFIRMATION_REQUIRED|PICKUP_CONFIRMATION_EXPIRED|ORDER_STATE_CONFLICT`；核验短信错误沿用 SMS_CODE_INVALID。

### 8.5 POST /api/v1/orders/{orderId}/after-sales

- 权限：U 本人；需幂等键。HTTP 201 返回 `AfterSaleDetail`。

| Body 字段 | 类型/必填 | 校验 |
|---|---|---|
| type | refund_before_pickup\|health_issue ! | 待自提退款 / 已交付健康申诉 |
| requestedResolution | full_refund\|exchange\|treatment_share ! | refund_before_pickup 仅 full_refund |
| reason | string ! | 1..1000 |
| diagnosisAt | datetime ? | health_issue 必填；机构诊断时间 |
| diagnosisFileIds | id[] ? | health_issue 必填，1..5 个诊断报告附件 |
| evidenceFileIds | id[] ? | 0..9 个补充图片/文档 |
| requestedAmount | amount ? | 仅 treatment_share 必填，>0 且≤剩余可退金额；仅申请值，最终以审核为准 |

默认健康申诉要求诊断及提交均在保障期内；报告晚于期限补交的处理方式待经营者确认，超期保留联系店主入口。服务器校验上传归属、用途、订单状态与截止时间；只验证材料齐全，诊断机构真实性/责任由店主审核。待自提售后申请期间保留宠物占用并阻塞交付。

OrderDetail.afterSaleEligibility 是前端展示方案的依据。exchangeEnabled=false 时不返回 exchange 选项，创建 exchange 申请或审核批准 exchange 都返回422 EXCHANGE_UNAVAILABLE；已创建的方案仍可查询并退出为退款，不能仅隐藏前端按钮。

自提超期任务若遇待审退款申请，复用该售后推进全额退款，不另开第二笔退款；即使此前用户请求被拒，超期自动退款规则仍适用。

### 8.6 售后 DTO 与查询

`AfterSaleSummary={id:id,orderId:id,type:string,status:string,requestedResolution:string,approvedResolution:string|null,createdAt:datetime,updatedAt:datetime}`。

`AfterSaleDetail = AfterSaleSummary + {version:integer,reason:string,diagnosisAt:datetime|null,diagnosisFileIds:id[],evidenceFileIds:id[],requestedAmount:amount|null,approvedAmount:amount|null,reviewReason:string|null,reviewedAt:datetime|null,returnRecord:ReturnRecord|null,exchange:Exchange|null,refund:Refund|null,timeline:TimelineEntry[]}`。

`TimelineEntry={event:string,label:string,note:string|null,occurredAt:datetime,actorType:buyer|admin|system}`；公开用户时间线不含内部诊断笔记、平台错误堆栈等内部字段。

**GET /api/v1/after-sales/{afterSaleId}**：U 本人，无 Query，返回 `AfterSaleDetail`。附件通过 9.2 权限访问。

**GET /api/v1/orders/{orderId}/after-sales**：U 本人，Query 为 page?/pageSize?，返回 `Page<AfterSaleSummary>`，创建时间倒序，用于同订单多次已结束售后的历史进度。用户从订单详情进入，不需要新的顶层导航。

### 8.7 GET /api/v1/admin/after-sales

- 权限：A。Query：`status?:3.4枚举,orderNo?:string,from?:datetime,to?:datetime,page?,pageSize?`，from≤to，范围≤366天。
- 返回 `Page<AfterSaleSummary>`，创建时间倒序。

### 8.8 GET /api/v1/admin/after-sales/{afterSaleId}

- 权限：A。返回 `AfterSaleDetail + {order:OrderDetail}`，仅按管理职责访问材料，记录审计。

### 8.9 POST /api/v1/admin/after-sales/{afterSaleId}/review

- 权限：A；幂等键。Body：`version!:integer,decision!:approve|reject,reason!:string(1..1000),resolution?:full_refund|exchange|treatment_share,approvedAmount?:amount`。
- reject 不允许 resolution/approvedAmount，结果 rejected；原订单恢复可用操作，已交付宠物仍 sold。
- approve 必须 resolution 且与 requestedResolution 一致；refund_before_pickup 只接受 full_refund。full_refund 金额由服务端取剩余可退额，不接受 approvedAmount；已交付则 awaiting_return，未交付直接发起退款。
- exchange 仅用于已交付 health_issue，进入 awaiting_return；treatment_share 必须 approvedAmount>0 且不超过申请值与剩余额，无需实物退回，直接部分退款。
- P0 不通过审核接口改变用户申请方案；需要协商变更时走明确的换宠转退款动作或线下联系处理，不能由 AI 自动审核。返回 `AfterSaleDetail`；涉及退款返回 202，否则 200。

### 8.10 POST /api/v1/admin/after-sales/{afterSaleId}/returns

- 权限：A；幂等键。Body：`version!:integer,received!:true,conditionNotes!:string(1..2000),evidenceFileIds?:id[](0..9)`。
- 仅 awaiting_return；店主确认原订单对应个体已退回，记录实际验收事实。服务端记录 receivedAt/operatorId，不能仅凭买家上传照片视为已退回。
- `ReturnRecord={id:id,receivedAt:datetime,conditionNotes:string,evidenceFileIds:id[],operatorId:id}`。
- 原宠物转 off，禁止因退款自动上架。全额退款方案创建退款并进入 refunding；换宠方案进入 awaiting_exchange。返回 `AfterSaleDetail`，有异步退款时 202。

### 8.11 PUT /api/v1/admin/after-sales/{afterSaleId}/exchange

- 权限：A；幂等键。Body：`version!:integer,replacementPetId!:id`。
- 仅 awaiting_exchange、原宠已退回；替换宠须 on_sale、证明有效、与原订单实付金额相同且原订单无成功部分退款。不同价返回 422 `EXCHANGE_PRICE_MISMATCH`，一期不设计差价支付接口。
- MySQL 事务锁定替换宠为 reserved，保存替换宠物/价格/检疫快照。更换已选择的替换宠须同时释放旧占用并锁定新占用，不允许覆盖已交付记录。
- 返回 `Exchange={id:id,replacementPetId:id,productSnapshot:object,status:reserved|completed|cancelled,confirmationId:id|null,confirmedAt:datetime|null,deliveredAt:datetime|null,deliveryQuarantine:object|null}`。

### 8.12 POST /api/v1/after-sales/{afterSaleId}/exchange-verification-codes

- 权限：U 本人。无 Body。仅存在有效 reserved 换宠方案可发送；返回 `{smsRequestId:id,phoneMasked:string,expiresIn:300,retryAfter:60}`，验证码绑定售后 ID、替换宠物 ID/版本、用途 exchange_confirm。

### 8.13 POST /api/v1/after-sales/{afterSaleId}/exchange-confirmations

- 权限：U 本人；幂等键。Body：`replacementPetId!:id,smsRequestId!:id,smsCode!:string,confirmationVersion!:string,confirmationContentHash!:string,accepted!:true,checks!:{mentalState:true,eyesAndNose:true,coat:true,excretion:true}`。
- 返回 `{confirmationId:id,confirmedAt:datetime,validUntil:datetime}`，有效 10 分钟。核验规则同 8.2；更换替换宠物后旧确认失效。

### 8.14 POST /api/v1/admin/after-sales/{afterSaleId}/exchange-delivery

- 权限：A；幂等键。Body：`version!:integer,confirmationId!:id,quarantineVerified!:true,deliveryQuarantine?:DeliveryQuarantine`，证明核对与更新规则同8.4，核验对象为替换宠。
- 同一事务校验新宠占用、有效检疫证明和对应买家有效确认，替换宠 sold、原宠保持 off，售后 resolved、exchange completed；交付证明公开快照保存到 exchange.deliveryQuarantine，保留原订单快照和原宠记录，不篡改 productId 为替换宠。
- 返回 `AfterSaleDetail`。设计默认不自动重开 7 天保障期，换宠后的保障期限与后续申诉规则属于待确认项；在规则确定前线上换宠入口应关闭，仅记录人工协商方案，不承诺完整换宠保障流程已可上线。

### 8.15 POST /api/v1/admin/after-sales/{afterSaleId}/exchange-refund

- 权限：A；需幂等键。Body：`version!:integer,reason!:string(1..1000),buyerConsentFileIds!:id[](1..5)`，证据用途 after_sale_evidence，保存与买家协商改为退款的记录。
- 仅原宠已退回、售后 awaiting_exchange 且替换宠未交付时可操作；事务内释放已有替换宠占用（符合可售条件→on_sale，否则off）、废止替换宠确认、改 approvedResolution 为 full_refund 并创建剩余金额原路退款。
- 返回 202 `AfterSaleDetail`。原宠仍 off，退款成功且复检通过后才允许人工上架。若替换宠交付已经成功，则拒绝转换，避免同一笔售后同时换宠和退款。
- 换宠选品、确认、交付接口在 exchangeEnabled=false 时统一返回 422 `EXCHANGE_UNAVAILABLE`；已存在的换宠方案仍允许查看和走本退款退出操作。

## 9. 后台运营、文件与基础统计

### 9.1 文件上传

**POST /api/v1/files**：U；**POST /api/v1/admin/files**：A。均为 `multipart/form-data`，表单 `file!:binary,purpose!:enum,orderId?:id`；不是 JSON，也不接受远程 URL 代替文件。

| purpose | 允许角色 | 内容与默认限制 | 可见性 |
|---|---|---|---|
| avatar | U | JPEG/PNG/WebP，≤2 MB | 公开，用于头像 |
| diagnosis | U | PDF/JPEG/PNG，≤10 MB；orderId 必填且属于本人 | 私有，诊断报告 |
| after_sale_evidence | U/A | PDF/JPEG/PNG，≤10 MB；orderId 必填且有权访问 | 私有，售后/退回证据 |
| pet_image / banner | A | JPEG/PNG/WebP，≤5 MB | 公开，宠物/轮播图 |
| pet_video | A | MP4，≤100 MB、≤60 秒 | 公开，详情视频 |
| quarantine_public | A | JPEG/PNG，≤10 MB | 公开，店主已处理可公开的证明图片 |
| quarantine_original | A | PDF/JPEG/PNG，≤10 MB | 私有，证明原件，只能管理员访问 |
| knowledge_import | A | CSV/XLSX，≤10 MB | 私有，知识导入 |

上传成功 201：`FileAsset={id:id,originalName:string,mimeType:string,sizeBytes:integer,purpose:string,visibility:public|private,status:"ready",publicUrl:string|null,createdAt:datetime}`。

后端校验真实文件类型、大小、图片/视频可解码性，使用随机存储 key；拒绝 SVG/HTML/可执行文件和伪装文件。不把原始文件名拼接为磁盘路径。私有文件不返回永久可访问 URL；公开 URL 只能是已验证的本站存储域名。业务写接口校验 fileId 的用途、上传者/关联订单和 ready 状态，不能只验证 ID 存在。

未被业务引用的临时上传设计默认 24 小时后清理；订单/售后证据和历史快照关联的文件不得被普通清理任务删除。证据与聊天等数据保留期限应分别确定，本文不提供任意删除历史交易证据的接口。

错误：400 VALIDATION_ERROR、404 RESOURCE_NOT_FOUND、413 FILE_TOO_LARGE、415 UNSUPPORTED_FILE_TYPE、422 FILE_PURPOSE_MISMATCH、429 RATE_LIMITED。

### 9.2 GET /api/v1/files/{fileId}/access-url

- 权限：U/A；无 Query。返回 `{fileId:id,url:string,expiresAt:datetime}`，私有 URL 默认 300 秒有效；`Cache-Control: no-store`。
- 买家只访问自己上传或其订单对外可见的售后材料，店主访问经营业务允许的附件；quarantine_original、knowledge_import 不向买家开放。他人文件统一 404。
- 临时签名 URL 是短期访问凭证，不入聊天 Prompt、埋点或公开页面；每次取链接都重新鉴权。公开文件直接使用 publicUrl，无需签名接口。

### 9.3 后台商品 DTO

`ProductWrite`：

| 字段 | 类型 | 创建要求/校验 |
|---|---|---|
| name / category / breed | string / cat\|dog / string | 必填；名称1..80、品种1..50 |
| priceAmount / gender | amount / male\|female | 必填；价格>0 |
| ageMonths / birthDate | integer / date\|null | 月龄必填0..360；出生日期可选且不得晚于今天 |
| weightKg / color | number / string | 必填；体重>0且≤200、两位小数；毛色1..30 |
| personalityTags | string[] | 默认[]，≤10项，每项1..20 |
| vaccineStatus / dewormStatus | string | 必填；分别1..500，不明应明确填写“待核实” |
| description / feedingNotes / healthDescription | string | 必填；分别1..10000 |
| imageFileIds | id[] | 必填，1..9 个 pet_image；第一个作为主图，顺序即展示顺序 |
| videoFileId | id\|null | 可选，pet_video |
| quarantine | object\|null | off 草稿允许 null；对象字段见下 |
| isRecommended / recommendationOrder | boolean / integer | 可选，默认 false/0；order≥0，越小越靠前 |

`quarantine={certificateNo:string(1..100),publicImageFileIds:id[](1..5),originalFileIds:id[](0..5),validUntil:date|null}`。validUntil 是有依据时录入的证明有效日期，不凭空生成；未填写日期时也必须在上架时人工核验有效性。编号、证据和健康核验属于商品个体，不得复制后自动视为另一只宠物的证明。

`AdminProduct = ProductWrite + {id:id,status:string,version:integer,activeOrderId:id|null,reviewedAt:datetime|null,lastSaleReviewNote:string|null,createdAt:datetime,updatedAt:datetime,publishedAt:datetime|null}`，额外返回 `assets:FileAsset[]` 用于后台预览。`birthDate` 非空时 ageMonths 由后端计算，输入月龄被忽略，返回计算值。

### 9.4 后台商品接口

所有接口权限 A。不存在返回 404；版本/状态冲突 409；证明/附件不符合要求 422。

| Method / Path | Query / Body | 返回 data / 规则 |
|---|---|---|
| `GET /api/v1/admin/pets` | Query：keyword?≤50、category?:cat\|dog、status?:四种宠物状态、page?/pageSize? | `Page<AdminProduct>`，创建时间倒序 |
| `GET /api/v1/admin/pets/{petId}` | 无额外参数 | `AdminProduct` |
| `POST /api/v1/admin/pets` | Body：ProductWrite | 201 `AdminProduct`，默认 off，不自动上架 |
| `PUT /api/v1/admin/pets/{petId}` | Body：完整 ProductWrite + version! | `AdminProduct`；仅 off/on_sale 可编辑，已关联订单快照不变 |
| `POST /api/v1/admin/pets/{petId}/publish` | Body：version!、healthyForSale!:true、quarantineVerified!:true、reviewNote!:string(1..1000) | `AdminProduct`；仅 off→on_sale；校验证明、价格、图片、无订单/换宠占用、退回后的原售后退款已成功或换宠已交付结束；记店主人工复核时间 |
| `POST /api/v1/admin/pets/{petId}/unpublish` | Body：version!、reason!:string(1..500) | `AdminProduct`；仅 on_sale→off；reserved/sold 拒绝 |

在售编辑如果改变证明/健康信息，应原子转 off，重新人工核验上架后再可购买；纯介绍、媒体、推荐位和价格编辑可维持 on_sale。更新已过期证明不能静默继续在售。状态决定是否可买，on_sale 仍需在创建订单时再检查有效性。

一期不提供物理删除宠物接口，避免丢失已购档案。需要隐藏用下架。推荐位编辑为 ProductWrite 的字段，不另设推荐算法接口。

### 9.5 后台订单查询

**GET /api/v1/admin/orders**：A；Query：`orderNo?:string(≤64),status?:3.2枚举,contactPhone?:phone,petId?:id,createdFrom?:datetime,createdTo?:datetime,page?,pageSize?`。时间范围≤366天、起点≤终点；返回 `Page<OrderSummary>`，创建时间倒序；按手机号搜索记录访问审计且不记明文到访问日志。

**GET /api/v1/admin/orders/{orderId}**：A；返回 `OrderDetail + {userId:id,contactPhone:phone}`。完整联系方式仅在此经营操作场景返回，前端不用于列表和埋点；若需系统间日志，统一脱敏。

管理后台不能通过 PATCH status 直接改支付成功/核销/退款成功。所有迁移只能调用对应业务动作和可信支付回调。

### 9.6 门店、Banner 设置

**GET /api/v1/admin/shop**：A；返回 `ShopSettings`。

**PUT /api/v1/admin/shop**：A；Body：完整 `ShopSettings` 的可写字段和 version；返回版本+1的新对象。

`ShopSettings = Shop + {version:integer,banners:AdminBanner[],paymentTimeoutMinutes:integer,pickupRetentionHours:integer,exchangeEnabled:boolean}`。`id` 只读不提交；默认付款30分钟、自提72小时；后台约束付款5..120分钟、自提1..168小时。exchangeEnabled 默认 false，换宠保障规则确定后才允许开通。

`AdminBanner={id:id|null,title:string(1..80),imageFileId:id,linkType:pet|notice|none,petId:id|null,noticeText:string|null,sortOrder:integer,enabled:boolean}`。列表≤10条；新条目 id=null、后端分配 ID，已存在条目保留 ID；PUT 中缺少的条目移出轮播。pet 类型必须 petId，notice 类型必须1..2000字符 noticeText，none 两者为 null。图片用途 banner。

店主修改协议保障期须发新协议版本，不能只改门店数字；旧订单的期限、正文和门店快照保持原值。支付 appId/商户号/证书、DeepSeek API Key 属于服务端运维配置，不通过普通门店设置接口读写。

### 9.7 协议版本维护

协议在 PRD 中必须展示且存签署证据；这里补充最小发布流程。已发布正文不可修改，通过新版本替代，历史版本永久可追溯。

| Method / Path | 权限与参数 | 返回 data |
|---|---|---|
| `GET /api/v1/admin/agreements` | A；Query：type?:live_pet_trade\|pickup_confirmation、page?/pageSize? | `Page<AgreementAdmin>`，创建时间倒序 |
| `GET /api/v1/admin/agreements/{agreementId}` | A；无 Query | `AgreementAdmin` |
| `POST /api/v1/admin/agreements` | A；Body：type!、version!:string(1..32)、title!:string(1..100)、content!:string(1..50000)、healthGuaranteeDays?:integer(1..30)；live_pet_trade 必填天数，pickup_confirmation 不传 | 201 `AgreementAdmin`；同 type+version 唯一，不覆盖旧版 |
| `POST /api/v1/admin/agreements/{agreementId}/publish` | A；Body：reviewConfirmed!:true；表明经营者已确认正文 | `AgreementAdmin`；原当前版本自动 retired，新版 published，事务内只保留一个当前版本；重复发布当前版本幂等成功，retired 版本不能重新发布（409 AGREEMENT_STATE_CONFLICT） |

`AgreementAdmin={id:id,type:string,version:string,title:string,content:string,contentFormat:"markdown",contentHash:string,healthGuaranteeDays:integer|null,status:draft|published|retired,createdAt:datetime,publishedAt:datetime|null}`。SHA-256 与 publishedAt 均后端生成；草稿正文需要修改时创建新版本，不开放覆写接口。未确认条款不调用发布接口。

发布新协议后，旧订单正常履行其已签署版本；结算尚未提交时版本变化返回 AGREEMENT_CHANGED。现场确认时固定当前 pickup_confirmation 文本并保存其版本；交易保障期仍取订单自己的快照。

### 9.8 GET /api/v1/admin/dashboard

- 权限：A。无 Query，统计门店时区“今天”。返回：

```json
{
  "date": "2026-09-06",
  "createdOrderCount": 8,
  "paidOrderCount": 3,
  "grossSalesAmount": 1680000,
  "refundAmount": 0,
  "netCashAmount": 1680000,
  "onSalePetCount": 18,
  "aiSessionCount": 26,
  "pendingTasks": { "pickupCount": 2, "afterSaleReviewCount": 1, "failedRefundCount": 0, "knowledgeFailedJobCount": 0, "paymentExceptionCount": 0 },
  "updatedAt": "2026-09-06T21:00:00+08:00"
}
```

createdOrderCount 按订单创建时间计数；paidOrderCount/grossSalesAmount 按当天支付成功事件，取消不算；refundAmount 按当天退款成功事件，可能退的是历史订单；netCashAmount=当日收款-当日退款，允许负数，属于现金流口径。aiSessionCount 按当天至少发起一条问题的去重会话数；onSalePetCount 和 pendingTasks 为查询时点值。设计默认允许60秒缓存，返回 updatedAt。

### 9.9 POST /api/v1/analytics/events

- 权限：U 或 G，二选一；未登录访问首页时可复用游客 Token。HTTP 202，返回 `{acceptedCount:integer,duplicateCount:integer}`。
- Body：`events!:AnalyticsEvent[](1..20)`，单请求≤32 KB；同身份 eventId 去重；无效字段整批400。用户ID由后端关联，不能客户端指定。
- `AnalyticsEvent={eventId:string(UUID),eventType:page_view|pet_detail_view|search,occurredAt:datetime,pagePath:string(≤200),petId:id|null,properties:object}`。
- page_view properties=`{page:home|list|detail|other,pathDepth:integer(0..20)}`；pet_detail_view 必需 petId，properties=`{pathDepth:integer(0..20)}`；search properties=`{keyword:string(1..50)}`，其余属性拒绝。客户端 occurredAt 与接收时间相差≤24小时，超出则标无效，不影响交易。
- pathDepth 定义为本次访问从首页/列表起到当前详情的页面跳转次数；刷新同一详情不增加。前端进入站点产生访问会话，连续30分钟不操作后重新计数；pagePath 不含 Query/订单号/自提码。
- `order_paid`、`order_refunded` 从后端支付事实采集；`session_end`、点踩、联系店主从 AI 业务接口采集，不能由浏览器伪造支付/会话结果。首字渲染耗时另见下述性能事件。

**POST /api/v1/analytics/ai-timings**：U/G，本人会话；Body：`eventId!:UUID,sessionId!:id,messageId!:id,firstRenderedMs!:integer(0..120000),completedRenderedMs?:integer(0..120000)`；消息必须为本会话助手消息。返回202 `{accepted:true}`，eventId 去重。客户端从发送问题到首字/完整渲染的耗时，服务端另采首字生成与总生成耗时；未出现首字不伪记为0，失败计数取消息状态。

### 9.10 统计口径与基线

- 详情 UV：统计期间内 pet_detail_view 的去重身份数；登录用户按 userId，游客按 guestToken 摘要。登录前后一期不自动合并，跨设备/清除凭证会造成重复，报表说明这一限制。
- 详情到支付比率：同期间支付成功订单数 ÷ 详情 UV；分母0返回 null。沿用 PRD 的订单数/UV 定义，可能大于100%，不冒充严格同一批访客的漏斗转化。
- AI 解决率：已结束会话中“结束时无点踩且无联系店主请求”的会话数 ÷ 全部已结束会话数；联系店主率为有联系请求的已结束会话 ÷ 已结束会话数，分母0返回 null。自动弹出联系选项不计作用户已经转人工。
- 会话显式 close 或默认30分钟无活动且无生成任务后由服务端关闭；关闭时固化统计快照。PRD 的该算法不能证明答案一定正确，另行保留 failed/unresolved 数量方便复盘。
- 采集连续4周后复核 PRD 的8%/85%/15%目标。一期保留上述采集和仪表盘，完整销量/访客/热门商品/AI报表放 P1。


## 10. AI 客服（P0）

本章路径均以 `/api/v1` 为根路径。知识问答、商品推荐支持游客；查订单必须使用买家 Bearer Token。管理员 Token 不代替买家身份。以下文本长度、文件限制、超时属于本版接口设计默认值，不是 PRD 已确认运营参数。

### 10.1 身份、会话与共享 DTO

同一请求只允许 `Authorization: Bearer <userToken>` 或 `X-Guest-Token: <guestToken>` 之一；同时传入返回 `400 AMBIGUOUS_IDENTITY`。游客凭证仅能操作自己创建的 AI 会话，不授予订单、售后、用户资料权限。登录后创建新的用户会话，不自动继承游客会话。

| DTO | 字段定义 |
|---|---|
| AiSession | `id:string`；`ownerType:user/guest`；`productId:string\|null`；`title:string`（服务端取首问摘要，≤50 字）；`status:active/closed`；`lastMessageAt:datetime\|null`；`createdAt:datetime`；`closedAt:datetime\|null` |
| AiSource | `knowledgeId:string`；`version:integer`；`title:string`；`sourceName:string`；`sourceUrl:string\|null`；`excerpt:string`（≤300 字）。来源取经过校验的知识库元数据，不接受模型自行生成的 URL |
| AiOrderCard | `orderId:string`；`orderNo:string`；`status:string`（复用订单状态枚举）；`productName:string`；`detailPath:string`（站内相对路径）；`nextAction:string\|null`（自然语言操作提示）。无联系人、手机号、自提码和支付参数 |
| AiAction | `type:login/contact_shop/view_products/view_order`；`label:string`；`path:string\|null`；`phone:string\|null`；`wechat:string\|null`。电话/微信来自门店配置，站内路径由后端构造；无任意模型 URL |
| AiMessage | `id:string`；`sequence:integer`（会话内单调递增）；`sessionId:string`；`role:user/assistant`；`clientMessageId:string`；`content:string`；`status:pending/streaming/completed/failed`；`intent:knowledge/order/product_recommendation/price/out_of_scope/chitchat/unknown\|null`；`outcome:answered/unresolved/action_required\|null`；`sources:AiSource[]`；`productCards:ProductCard[]`；`orderCards:AiOrderCard[]`；`actions:AiAction[]`；`suggestedQuestions:string[]`（最多3项）；`feedback:up/down/none`；`disclaimer:string\|null`；`errorCode:string\|null`；`createdAt:datetime`；`completedAt:datetime\|null` |

`ProductCard` 复用商品模块定义，金额为 `priceAmount`（整数分，CNY）。用户消息 `intent/outcome/disclaimer/completedAt` 可为 null，其余集合为空数组；已保存的用户消息 `status=completed`。所有 AI 回复的 `disclaimer` 固定为“AI 生成，仅供参考”；涉及健康症状必须补充“建议及时就医”。`sources` 保存引用时的标题、版本与摘要快照，后续知识编辑不篡改历史回答。

### 10.2 创建游客身份

`POST /api/v1/guest-sessions`，权限：公开；无 Query、无 Body。响应 `201`，`data`：

```json
{"guestToken":"<opaque-random-token>","expiresAt":"2026-09-07T21:00:00+08:00"}
```

服务端生成至少 256 bit 随机不透明凭证，仅存摘要，24 小时过期；响应 `Cache-Control: no-store`。客户端不放入 URL 或分析日志；过期后旧游客会话不能凭新 Token 访问。按 IP 与设备请求频率限流，超限 `429 RATE_LIMITED`，带 `Retry-After`。本接口不创建正式用户账号。

### 10.3 创建、查询与结束会话

权限：买家或游客，必须满足 10.1 身份规则；他人或不存在会话统一返回 `404 AI_SESSION_NOT_FOUND`。

| Method / Path | Query / Body | 返回 data / 关键错误 |
|---|---|---|
| `POST /ai/sessions` | Body：`productId:string` 可选；仅接受当前公开可见商品 ID，用于带入品种上下文 | `201 AiSession`；无效商品 `404 PRODUCT_NOT_FOUND` |
| `GET /ai/sessions` | Query：`page:integer` 可选，默认1，≥1；`pageSize:integer` 可选，默认20，1..100；`status:active/closed` 可选 | `200 {items:AiSession[],page,pageSize,total}`；按 `lastMessageAt`（空值取创建时间）、ID 倒序，只列当前身份会话 |
| `GET /ai/sessions/{sessionId}` | Path：`sessionId:string` 必填；无 Query/Body | `200 AiSession` |
| `GET /ai/sessions/{sessionId}/messages` | Path 同上；Query：`page/pageSize` 同上；`afterSequence:integer` 可选，≥0，仅取序号大于此值的消息 | `200 {items:AiMessage[],page,pageSize,total}`；按 sequence 升序；用于历史与断流恢复 |
| `GET /ai/sessions/{sessionId}/messages/{messageId}` | Path：两个 ID 必填；无 Query/Body | `200 AiMessage`；读取单条生成状态及已保存内容；不存在或不属于本会话 `404 AI_MESSAGE_NOT_FOUND` |
| `POST /ai/sessions/{sessionId}/close` | Path 同上；无 Query/Body | `200 AiSession`；幂等关闭；生成中返回 `409 AI_MESSAGE_IN_PROGRESS`，完成或失败后可关闭 |

创建示例：`{"productId":"pet_1001"}`；普通知识问答传 `{}`。响应示例：

```json
{"code":"OK","message":"success","data":{"id":"chat_1001","ownerType":"guest","productId":"pet_1001","title":"新会话","status":"active","lastMessageAt":null,"createdAt":"2026-09-06T21:00:00+08:00","closedAt":null},"requestId":"req_demo","serverTime":"2026-09-06T21:00:00+08:00"}
```

### 10.4 发送问题并流式接收回答

`POST /api/v1/ai/sessions/{sessionId}/messages`，权限：会话所属买家或游客。Path `sessionId:string` 必填；无 Query；请求头 `Content-Type: application/json`、`Accept: text/event-stream`。

| Body 字段 | 类型 | 必填 | 校验 / 说明 |
|---|---|---|---|
| clientMessageId | string | 是 | UUID，由客户端为一次提问生成；同一逻辑请求重试必须保持原值 |
| content | string | 是 | 去除首尾空白后 1..2000 字；不接收客户端拼接的系统 Prompt |
| orderId | string | 否 | 明确查询某笔订单时传入；必须是当前买家本人订单；游客提交返回 `401 LOGIN_REQUIRED` |

发送示例：

```json
{"clientMessageId":"b8caeddf-a6a2-4a70-8ad2-7c26d52a1742","content":"这只布偶猫应该如何梳毛？"}
```

每个会话同一时刻最多一条生成中的 assistant 消息，新的不同请求返回 `409 AI_MESSAGE_IN_PROGRESS`。`(sessionId,clientMessageId)` 建唯一约束：相同 ID、相同规范化 Body 只对应原来的一组用户/助手消息，不重复扣模型调用次数；相同 ID 不同 Body 返回 `409 IDEMPOTENCY_CONFLICT`。原请求生成中时重试返回 `409 AI_MESSAGE_IN_PROGRESS`；已完成/失败时按已持久化内容重放 SSE，不重新生成。幂等记录与消息保存期一致。

身份、会话归属、字段、明确指定订单归属、并发与幂等检查必须在开启 SSE 前完成。失败按统一 JSON 错误响应返回真实 HTTP 状态：`400 VALIDATION_ERROR`、`401 UNAUTHORIZED/LOGIN_REQUIRED`、`404 AI_SESSION_NOT_FOUND/ORDER_NOT_FOUND`、`409 AI_SESSION_CLOSED/AI_MESSAGE_IN_PROGRESS/IDEMPOTENCY_CONFLICT`、`429 RATE_LIMITED`。

成功握手 `200 Content-Type: text/event-stream; charset=utf-8`，同时返回 `Cache-Control: no-cache, no-transform`、`X-Accel-Buffering: no`。SSE 的 `data` 为下表事件对象，不嵌套统一响应外壳；`meta` 携带 `requestId`，终态事件携带服务器时间。客户端用 `fetch` 读取流，原生 `EventSource` 不支持本接口的 POST Body 与鉴权方式。

| event | data 结构 | 顺序与语义 |
|---|---|---|
| meta | `{requestId,sessionId,clientMessageId,userMessageId,assistantMessageId,replayed:boolean}` | 第一条，表示用户消息与助手占位记录已提交数据库 |
| delta | `{messageId,text:string}` | 0..N 条，仅追加正文；客户端按接收顺序拼接，不把片段当 HTML 执行 |
| sources | `{messageId,items:AiSource[]}` | 0..1 条，实际采用的引用来源；无来源时可不发 |
| cards | `{messageId,productCards:ProductCard[],orderCards:AiOrderCard[]}` | 0..1 条，商品与订单卡片均由业务后端生成 |
| action | `{messageId,items:AiAction[]}` | 0..1 条，登录、查看商品、查看订单或联系店主操作 |
| done | `{messageId,status:"completed",intent,outcome,suggestedQuestions:string[],disclaimer,serverTime}` | 成功终态；发送前已持久化完整正文、卡片、引用、操作和结果 |
| error | `{messageId,status:"failed",code,message,retryable:boolean,actions:AiAction[],serverTime}` | 失败终态；错误与部分正文已落库；随后关流，无 done |

除第一条 `meta`、最后一条 `done/error` 外，业务事件不要求固定相互顺序。无正文的订单卡片/登录引导也必须发送 `done`。服务端可发送 `: ping` 心跳注释，客户端忽略。`sources/cards/action` 均为该类数据的完整集合，重放时替换集合，不追加重复卡片。

```text
event: meta
data: {"requestId":"req_demo","sessionId":"chat_1001","clientMessageId":"b8caeddf-a6a2-4a70-8ad2-7c26d52a1742","userMessageId":"msg_1001","assistantMessageId":"msg_1002","replayed":false}

event: delta
data: {"messageId":"msg_1002","text":"布偶猫可以从轻柔的日常梳毛开始。"}

event: sources
data: {"messageId":"msg_1002","items":[{"knowledgeId":"kb_1001","version":1,"title":"布偶猫日常梳毛","sourceName":"门店护理手册","sourceUrl":null,"excerpt":"先轻柔梳理毛发，发现皮肤异常时及时咨询兽医。"}]}

event: done
data: {"messageId":"msg_1002","status":"completed","intent":"knowledge","outcome":"answered","suggestedQuestions":["猫咪出现毛结怎么办？"],"disclaimer":"AI 生成，仅供参考","serverTime":"2026-09-06T21:00:03+08:00"}

```

断流处理：客户端收到 `meta` 后即持有 `assistantMessageId`，断流先调用单条消息 GET 接口确认状态；生成中每2秒拉取，终态以已保存记录为准，再同步消息历史。不自动重发新 ID，不自动重新生成；若用户明确再次提问，则生成新的 `clientMessageId`。未收到 `meta` 时，用原 ID 重试确认，仍遵守幂等规则。历史增量拉取的 `afterSequence` 不会重新返回已有的流式消息，恢复中不得只依赖增量条件。

服务端在连接断开后继续受控生成并保存；生成超时默认 30 秒，任务被中断或上游异常时，将占位消息改为 `failed`。SSE 已开始后的 HTTP 状态不能改为 5xx，必须用 `error` 事件；错误码 `AI_UPSTREAM_UNAVAILABLE/AI_GENERATION_TIMEOUT`，给出联系店主操作，不泄露模型密钥、Prompt、堆栈或供应商内部响应。

### 10.5 意图执行、反馈与兜底

订单查询走订单 Service，身份只从服务端认证上下文读取；不得让模型指定 `userId`、直接拼 SQL 或绕过归属校验。明确 `orderId` 非本人时统一 `404 ORDER_NOT_FOUND`。用户未指定订单号时，后端最多提供最近 5 笔本人订单卡片并引导到订单页；订单状态与下一步操作由后端模板生成。订单联系人、自提码、完整订单记录均不进入模型上下文和向量库。

游客自然语言询问“我的订单”时不调用订单服务，返回 `intent=order`、`outcome=action_required` 与 `login` action；若请求体直接携带 `orderId`，在握手前返回 `401 LOGIN_REQUIRED`。健康问答仅提供参考建议与就医提示；异宠问题回复“目前知识库仅覆盖猫和狗，其他宠物问题请联系店主”。价格问题不生成市场行情数字，返回 `view_products` action；商品卡片只能展示 MySQL 当前公开售价。

推荐先做知识/商品候选检索，再从 MySQL 复核商品当前状态、价格、公开可见性；只推荐 `on_sale` 商品。商品变更事件触发索引同步，旧向量不得使已售、下架商品成为推荐结果。知识引用也须复核条目当前版本、发布状态；不得引用被删除、归档或尚未完成索引的新版本。

| Method / Path | 权限及输入 | 返回 data / 关键错误 |
|---|---|---|
| `PUT /ai/sessions/{sessionId}/messages/{messageId}/feedback` | 本人买家/游客会话；Path 两个 ID 必填；无 Query；Body：`value:up/down/none` 必填；`reason:string` 可选，≤500 字，仅 down 可填 | `200 {messageId,value,reason:string\|null,updatedAt}`；同值重复提交不重复计数；他人/不存在消息 `404 AI_MESSAGE_NOT_FOUND`；非完成的助手消息 `409 AI_MESSAGE_NOT_FEEDBACKABLE`；会话关闭后 `409 AI_SESSION_CLOSED` |
| `POST /ai/sessions/{sessionId}/contact-shop` | 本人买家/游客会话；Path sessionId 必填；无 Query；Body：`messageId:string` 可选，须为本会话助手消息 | `200 {phone:string,wechat:string,contactRequested:true}`；读取门店最新联系方式，活动会话按会话幂等记录联系店主意愿；已关闭会话仍返回联系方式但不修改结束时统计快照；错误 `404 AI_SESSION_NOT_FOUND/AI_MESSAGE_NOT_FOUND` |

反馈示例：`{"value":"down","reason":"没有解释幼猫如何适应梳毛"}`；撤销为 `{"value":"none"}`。后端判断“连续两次未解决”：最近两个已结束的助手回复均为 `outcome=unresolved` 或当前反馈为 `down`，同一回复不重复计数；触发后返回/展示 `contact_shop`，页面也常驻联系店主入口。反馈后前端可拉取会话最近消息，读取重新计算的最后一条回复 `actions`。

会话关闭时固化 `closedAt`、是否存在点踩、是否请求联系店主、消息数、首字与完整响应耗时，供后续统计使用。客户端点击电话/微信前调用 `contact-shop`；接口失败不应阻止用户拨号。实际外部联系是否解决问题不冒充已验证结果。未解决问题保留在聊天记录中，P0 由店主审核整理为 `format=qa` 的知识条目，不将未经审核的历史 AI 回答自动发布。

AI 会话记录后台检索、解决率报表、未解决问题运营页及实时人工接管属于 P1；P0 的“联系店主”返回电话/微信，不建设在线客服坐席、消息推送或人工会话路由。

### 10.6 AI 联调验收用例

| 输入 / 场景 | 应验收的结果 |
|---|---|
| 游客询问布偶猫梳毛 | 可创建会话；按 meta → 业务事件 → done 收流；历史保存正文、引用与提示语 |
| 游客询问订单；买家 A 指定买家 B 的 orderId | 前者得到登录 action；后者握手前 404，均不能查到订单内容 |
| 相同 clientMessageId 重试、断流后恢复 | 仅一组消息、一次生成；历史记录可定位完成/失败状态 |
| 推荐候选刚售出或刚改价 | 最终卡片排除已售商品，价格等于 MySQL 当前值 |
| 连续两条无法解决、健康问题、异宠、行情价格 | 分别联系店主、提示就医、明确猫狗范围、引导在售商品页 |

## 11. 知识库管理（P0，管理员）

本章全部要求管理员 Bearer Token，买家和游客返回 `403 FORBIDDEN`（未登录返回 `401 UNAUTHORIZED`）。路径根为 `/api/v1/admin`。知识条目管理、模板下载、导入、索引任务均不得使用用户侧 Token。

### 11.1 知识条目与字段

`KnowledgeEntry` 返回下列写入字段及只读字段。ID 均为 string，日期为带时区 ISO 8601。枚举范围固定覆盖猫狗；不新增异宠类目。

| 写入字段 | 类型 | 创建必填 | 校验与说明 |
|---|---|---|---|
| title | string | 是 | 去首尾空白后 1..120 字 |
| category | enum | 是 | `breed/feeding/training/health/grooming`；分别为品种百科、饲养、训练、健康、美容 |
| petType | enum | 是 | `cat/dog/both` |
| breedNames | string[] | 否 | 默认 []；最多20项，每项1..50字、去重；空数组代表不限品种 |
| format | enum | 是 | `article/qa`；问答对与长文共用管理接口 |
| content | string | 条件 | article 必填，1..30000字；qa 时必须不传或为 null |
| question | string | 条件 | qa 必填，1..2000字；article 时必须不传或为 null |
| answer | string | 条件 | qa 必填，1..10000字；article 时必须不传或为 null |
| sourceName | string | 是 | 1..200字，例如“门店护理手册”；历史问答需标记“历史问答，经店主审核” |
| sourceUrl | string/null | 否 | 默认 null；≤2048字；只接受无账号密码、无鉴权密钥的 HTTPS 公共来源 URL；填写仅作引用，不自动抓取 |
| status | enum | 否 | 默认 `draft`；允许 `draft/published/archived`；发布代表店主已审核内容 |

只读字段：`id:string`、`version:integer`（从1开始）、`indexStatus:not_indexed/pending/indexing/ready/failed`、`indexedVersion:integer|null`、`indexErrorCode:string|null`、`createdAt:datetime`、`updatedAt:datetime`。`content/question/answer` 不适用字段在响应中为 null。

`breed` 长文按“品种名、原产地、体型、寿命、性格、毛色、饲养要点、常见疾病”组织正文即可，不另建动态字段系统。品种性格、毛色品相归入 `breed`；历史问答用 `format=qa` 并选择对应业务分类。

### 11.2 条目 CRUD

| Method / Path | Query / Body | 返回 data / 关键错误 |
|---|---|---|
| `GET /knowledge/entries` | Query：`page:integer` 默认1且≥1；`pageSize:integer` 默认20且1..100；`keyword:string` 可选≤100字；`category/petType/format/status/indexStatus` 可选，枚举同上；keyword 匹配标题、正文或问答内容，petType 为精确匹配 | `200 {items:KnowledgeEntry[],page,pageSize,total}`；按 updatedAt、ID 倒序 |
| `GET /knowledge/entries/{entryId}` | Path：`entryId:string` 必填；无 Query/Body | `200 KnowledgeEntry`；`404 KNOWLEDGE_NOT_FOUND` |
| `POST /knowledge/entries` | 无 Query；Body 按 11.1 创建字段 | `201 KnowledgeEntry`；`400 VALIDATION_ERROR` |
| `PUT /knowledge/entries/{entryId}` | Path entryId 必填；无 Query；Body：11.1 完整可写字段 + `version:integer` 必填，≥1，须等于读取版本；可选字段省略恢复默认值 | `200 KnowledgeEntry`（version+1）；`409 VERSION_CONFLICT`；`404 KNOWLEDGE_NOT_FOUND` |
| `DELETE /knowledge/entries/{entryId}` | Path entryId 必填；Query：`version:integer` 必填，≥1；无 Body | `200 {id,deleted:true}`；逻辑删除并移除检索资格；`409 VERSION_CONFLICT`；`404 KNOWLEDGE_NOT_FOUND` |

article 创建示例：

```json
{"title":"布偶猫日常梳毛","category":"grooming","petType":"cat","breedNames":["布偶猫"],"format":"article","content":"使用合适的梳具轻柔梳理，避免拉扯皮肤。发现皮肤红肿时建议及时就医。","sourceName":"门店护理手册","sourceUrl":null,"status":"published"}
```

qa 创建示例：

```json
{"title":"幼犬基础训练如何开始","category":"training","petType":"dog","breedNames":[],"format":"qa","question":"幼犬如何开始学习坐下？","answer":"在安静环境中短时练习，通过奖励引导，不采用惩罚方式。","sourceName":"历史问答，经店主审核","status":"draft"}
```

article 响应 data 示例：

```json
{"id":"kb_1001","version":1,"title":"布偶猫日常梳毛","category":"grooming","petType":"cat","breedNames":["布偶猫"],"format":"article","content":"使用合适的梳具轻柔梳理，避免拉扯皮肤。发现皮肤红肿时建议及时就医。","question":null,"answer":null,"sourceName":"门店护理手册","sourceUrl":null,"status":"published","indexStatus":"pending","indexedVersion":null,"indexErrorCode":null,"createdAt":"2026-09-06T21:00:00+08:00","updatedAt":"2026-09-06T21:00:00+08:00"}
```

发布或编辑已发布条目后自动异步更新向量索引；写接口成功仅代表 MySQL 已保存，不能直接宣称向量已可检索。只有 `status=published` 且 `indexStatus=ready` 且 `indexedVersion=version` 才能进入新问答；更新期间该条目暂不检索，避免把旧事实用于新回答。草稿、归档、逻辑删除立即从业务检索过滤条件中排除，向量物理清理可异步完成。单条索引失败可保存内容后重试或使用重建接口。

### 11.3 下载 Excel / CSV 导入模板

`GET /api/v1/admin/knowledge/import-template`；Query `format:xlsx/csv` 必填；无 Body。成功 `200` 直接下载二进制或文本文件，**不包统一 JSON 外壳**。响应头 `Content-Disposition: attachment; filename="knowledge-import-template.xlsx"`（CSV 对应 `.csv`）；Content-Type 分别为 `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`、`text/csv; charset=utf-8`。非法格式 `400 VALIDATION_ERROR`（JSON 错误外壳）。

XLSX 工作表名为 `knowledge`；CSV 使用 UTF-8（允许 BOM）、英文逗号与 RFC 4180 引号转义。列头固定为：

```csv
title,category,petType,breedNames,format,content,question,answer,sourceName,sourceUrl
布偶猫日常梳毛,grooming,cat,布偶猫,article,轻柔梳理，发现皮肤异常时建议就医。,,,门店护理手册,
幼犬基础训练,training,dog,,qa,,幼犬如何学习坐下？,在安静环境中短时练习并使用奖励引导。,历史问答，经店主审核,
```

模板包含以上两条示例，使用前替换或删除。`breedNames` 多值用英文分号分隔；字符串内容包含英文逗号、双引号或换行时须按 CSV 规则转义。各列校验复用 11.1；`content/question/answer` 按 format 条件必填。导入只新增草稿，不接受 ID、版本号和发布状态列，不静默覆盖现有知识。

### 11.4 异步批量导入

先通过 `POST /api/v1/admin/files` 上传，multipart 字段 `file` 与 `purpose=knowledge_import`，取得 `fileId`（即上传响应的 `data.id`）。任务受理时建立文件引用，排除第9.1章未引用临时文件清理。仅接受 `.xlsx/.csv`，单文件≤10 MB、最多5000条非空数据行；后台同时校验扩展名、MIME、实际格式与解压后体积，不执行公式、宏或外部链接。不使用买家附件，不接受任意远程下载 URL。

`POST /api/v1/admin/knowledge/import-jobs`；无 Query；Header `Idempotency-Key:string` 必填，UUID；Body：

| 字段 | 类型 | 必填 | 校验 |
|---|---|---|---|
| fileId | string | 是 | 当前管理员可使用、purpose=knowledge_import、上传完成且未失效的文件 |

请求：`{"fileId":"file_kb_1001"}`。响应 `202`，`data=KnowledgeJob`，`Location: /api/v1/admin/knowledge/jobs/kjob_1001`。同管理员、同接口、同幂等键和 Body 返回原任务；不同 Body 返回 `409 IDEMPOTENCY_CONFLICT`。幂等记录至少保留7天，与第2.5章一致。

同步拒绝：`400 VALIDATION_ERROR`、`404 FILE_NOT_FOUND`、`409 FILE_NOT_READY`、`413 FILE_TOO_LARGE`、`415 UNSUPPORTED_FILE_TYPE`、`422 FILE_PURPOSE_MISMATCH`、`429 RATE_LIMITED`。文件结构、行数上限、单元格内容等解析后错误体现在任务中；文件头不符、超行数或文件损坏导致整批失败，创建任何条目前先完成文件结构检查。

行级校验通过的行新增草稿，失败行不写入；存在有效和失败行时 `partial_succeeded`，全部失败时 `failed`，全部成功时 `succeeded`。任务内部以 `(jobId,rowNumber)` 唯一约束避免工作进程重试重复插入；用户重新上传并创建新任务仍会新增条目，前端应明确提示。发布需店主在条目编辑页审核后单独执行 PUT。

### 11.5 索引重建、商品同步与任务查询

| Method / Path | Query / Body | 返回 data / 关键错误 |
|---|---|---|
| `POST /knowledge/index-rebuild-jobs` | 无 Query；Header `Idempotency-Key:string` 必填，UUID；Body：`scope:knowledge/products/all` 必填 | `202 KnowledgeJob`；同一 scope 已有冲突的运行任务 `409 KNOWLEDGE_JOB_RUNNING`；同键异参 `409 IDEMPOTENCY_CONFLICT` |
| `POST /knowledge/product-sync-jobs` | 无 Query；Header Idempotency-Key 同上；Body `{}`，全量校准商品索引 | `202 KnowledgeJob`；商品索引已有冲突任务 `409 KNOWLEDGE_JOB_RUNNING` |
| `GET /knowledge/jobs/{jobId}` | Path：`jobId:string` 必填；无 Query/Body | `200 KnowledgeJob`；`404 KNOWLEDGE_JOB_NOT_FOUND` |
| `GET /knowledge/jobs/{jobId}/errors` | Path jobId 必填；Query：`page:integer` 默认1且≥1，`pageSize:integer` 默认20且1..100；无 Body | `200 {items:KnowledgeJobError[],page,pageSize,total}`；按 rowNumber/条目ID排序；无错误时空数组；`404 KNOWLEDGE_JOB_NOT_FOUND` |

新建索引任务的成功响应同样带 `Location`。重建使用快照版本与独立新索引，完成后切换读取版本；重建失败保留原可用索引。扫描期间的知识修改、删除、商品价格/状态变化通过变更事件补齐，并在切换前校准，不让旧快照覆盖新数据。提交任务不阻塞交易业务。

商品创建、上架、改价、修改品种/特征、下架、预订、售出触发自动同步；`product-sync-jobs` 用于补偿失败与全量校准，`index-rebuild-jobs scope=products` 用于重建商品向量。重叠任务互斥：`all` 与任意重建/同步冲突；`products` 与商品同步冲突；纯知识重建与商品同步可并行。商品索引只含公开商品知识，不包含订单、联系方式、自提凭证或买家信息。推荐接口最终仍以 MySQL 实时复核为准。

| KnowledgeJob 字段 | 类型与含义 |
|---|---|
| id / type / scope | `string`；`import/index_rebuild/product_sync`；`knowledge/products/all`（导入固定 knowledge，商品同步固定 products） |
| status | `queued/running/succeeded/partial_succeeded/failed`；partial_succeeded 仅用于导入 |
| totalCount | integer/null；解析/扫描总数前为 null，已知后≥0 |
| processedCount / successCount / failedCount | integer，≥0；processedCount=successCount+failedCount；导入均按非空数据行计数，重建/同步按数据条目计数 |
| fileId | string/null；仅导入任务有值 |
| errorCode / errorMessage | string/null；任务级错误，如 `IMPORT_INVALID_HEADER/IMPORT_ROW_LIMIT_EXCEEDED/IMPORT_FILE_INVALID/KNOWLEDGE_INDEX_FAILED` |
| createdAt / startedAt / finishedAt | datetime；后两者未到对应阶段为 null |

`KnowledgeJobError`：`rowNumber:integer|null`（导入时表头为第1行，数据从第2行起）、`entryId:string|null`（知识/商品 ID）、`field:string|null`、`code:string`、`message:string`。例如 `{"rowNumber":3,"entryId":null,"field":"answer","code":"VALIDATION_ERROR","message":"format 为 qa 时 answer 必填"}`。错误信息不回传整个上传行、不包含供应商凭据。

任务响应示例：

```json
{"code":"OK","message":"success","data":{"id":"kjob_1001","type":"import","scope":"knowledge","status":"queued","totalCount":null,"processedCount":0,"successCount":0,"failedCount":0,"fileId":"file_kb_1001","errorCode":null,"errorMessage":null,"createdAt":"2026-09-06T21:00:00+08:00","startedAt":null,"finishedAt":null},"requestId":"req_demo","serverTime":"2026-09-06T21:00:00+08:00"}
```

前端每2秒查询任务，进入终态后停止。重建/同步任务只有全部必需条目处理成功并完成切换/校准才可标为 succeeded；失败时展示可读原因与任务 ID，由管理员修复数据或依赖后以新幂等键发起重试。后台导入、编辑、发布、删除、重建和同步均记录管理员 ID、对象 ID、操作时间、结果及 requestId。

### 11.6 知识库联调验收用例

| 输入 / 场景 | 应验收的结果 |
|---|---|
| 下载两个格式模板，各填入一条长文与一条 qa | XLSX/CSV 都可导入，生成草稿；审核发布且索引就绪后才可被问答引用 |
| 一个有效行、一个缺少 answer 的 qa 行 | 任务 partial_succeeded，仅有效行入库，错误列表准确标出行号和字段 |
| 修改已发布知识后立即提问，再删除该知识 | 旧版本不参与新检索；删除即时禁止引用；历史答案来源快照仍保留 |
| 重建中修改商品价格，或重建中索引服务失败 | 成功切换后保留最新值；失败保留原可用索引，实时业务过滤仍生效 |

## 12. P1 与二期边界

以下只列后续能力，不是已经冻结的 v1 接口契约；前端一期不展示不可用按钮，也不应以本章路径自行开始开发。

| 能力 | PRD 优先级/裁剪 | 一期处理 |
|---|---|---|
| 收藏、我的收藏 | P1 | 后置，不设收藏写接口 |
| 我的宠物档案 | P1 | 已购宠物从订单快照可追溯；独立档案页和 AI 档案联动后置 |
| 热门搜索词配置 | P1 | 一期只采集搜索关键词，不提供运营配置页 |
| 评价、回复、评价管理 | 第10章明确二期 | 一期不设计评价权限和审核流程 |
| 售后被拒补材料再申诉 | P1 | 一期保留电话/微信协商；不覆盖原驳回记录 |
| AI 会话后台、未解决问题运营页、实时人工接管 | P1 | 一期返回门店联系方式，保留后续可用的会话与事件数据 |
| 用户列表/用户档案管理 | P1 | 一期后台通过订单查看必要联系信息 |
| 完整销量/访客/转化/热门商品/AI报表 | P1 | 一期仅仪表盘和最小事件采集 |
| 购物车、支付宝、优惠券、PC买家端 | 二期或明确裁剪 | v1 无对应接口；PC店主后台仍属于一期 |
| 多商家、物流配送、直播、社区、异宠知识、行情报价 | 不在一期范围 | 不创建占位业务或伪数据接口 |

退出登录虽在 PRD 列为 P1，本文与会话撤销一起提供，避免登录只有进入没有退出。协议发布、支付能力查询、异步任务结果属于 P0 功能可运行所需的支撑契约，不是新增营销功能。

## 13. 需求覆盖与联调验收

### 13.1 需求到接口的映射

| PRD 条目 | 本文位置 | 开发产出 |
|---|---|---|
| 4.1.0 账号登录 | 4.1～4.11 | 短信、微信授权/绑定、买家/店主登录和退出 |
| 4.1.1 首页 | 5.2～5.4、9.6 | 轮播、分类、手动推荐、门店、客服入口所需数据 |
| 4.1.2 列表搜索 | 5.5 | 分类/品种/价格/性别/年龄/毛色筛选、四种排序 |
| 4.1.3 详情 | 5.1、5.6～5.7、9.1 | 图/视频、档案、健康与检疫证明、协议、自提说明 |
| 4.1.4 结算支付 | 6.1～6.3、7.1～7.4 | 登录检查、服务端计价、签署、唯一锁定、微信双场景 |
| 4.1.5 订单 | 6.3～6.6、8.1～8.4 | 列表详情、取消、超时、买家核验、店主交付 |
| 4.1.6 个人信息/联系店主 | 4.8～4.9、5.2 | 头像昵称、脱敏手机号、公开门店联系方式 |
| 4.1.7、4.4 售后/保障 | 第8章、9.1～9.2、9.7 | 诊断材料、审核、退回、退款、换宠契约、签署证据 |
| 4.2 AI 客服 | 第10章 | 游客/买家会话、SSE、引用、商品卡片、本人订单、反馈兜底 |
| 4.2.5 知识更新 | 第11章 | 文章/问答对、模板与导入、向量重建、商品同步 |
| 4.3 后台商品/订单/门店/仪表盘 | 8.3～8.15、第9章 | 经营操作和待办数据 |
| 4.5 一宠一档/状态机 | 第3章、6.2、6.6、第7～8章 | 数据库约束、支付关单/退款事实和库存迁移 |
| 1.3 指标基线 | 9.8～9.10、10.5 | 后端支付事实、详情UV、AI结束快照、首字埋点 |
| 第5章非功能要求 | 2.1～2.5、7.4、9.1、10.4 | 鉴权、脱敏、限流、回调、流式输出和超时恢复 |

### 13.2 关键联调用例

下面是实现后必须执行的验收用例。本文仅核对了文档契约，尚未执行这些服务测试。

| 编号 | 场景/操作 | 预期结果 |
|---|---|---|
| A01 | 同一手机号重复短信登录、微信绑定已存在手机号 | 一个买家账号；短信用途不可串用；冲突身份不覆盖 |
| A02 | 修改 orderId/afterSaleId/fileId/sessionId 为他人的 ID | 404；不返回他人的任何资源内容；买家 Token 无后台权限 |
| A03 | 两个买家并发对同一宠物下单 | 仅一个成功；另一个 PET_NOT_AVAILABLE；无孤立订单/锁 |
| A04 | 同 key 重试创建/不同 Body 重用 key | 相同逻辑只创建一次；不同 Body 409 |
| A05 | 预览后改价、更新协议或被他人预订 | 创建失败并返回明确错误，不按客户端金额成交 |
| A06 | 取消与微信支付成功通知并发 | 查关单确认后取消或转退款；不能重复出售已支付宠物 |
| A07 | 本地30分钟任务触发，平台仍付款处理中 | 保持 closing、待核查；Redis key 过期不直接上架 |
| A08 | 前端 SDK 声称成功，但平台查单未成功 | 本地不置 paid、不生成可用自提凭证 |
| A09 | 同一支付通知重复/乱序；签名或金额不匹配 | 合法重复幂等；不匹配不入账，库存不发生错误迁移 |
| A10 | 在微信内/外部移动浏览器各发起支付 | 分别 JSAPI/H5；未开通能力返回禁用原因；真实商户环境单独验收 |
| A11 | 买家未确认、确认过期、验证码用途错、店主重复核销 | 拒绝无效核销；合法重复不再次交付或重算保障期 |
| A12 | 店主核销与72小时超期退款任务并发 | 只成功一个分支；进入退款后不能交付 |
| A13 | 退款请求超时、平台退款失败、成功通知重复 | 结果未知先查单；失败不释放；成功只累计一次 |
| A14 | 两个退款并发/部分治疗费退款 | 成功+处理中的退款不超已付额；部分成功不直接置 refunded |
| A15 | 买家提交健康申诉后被驳回 | 宠物仍 sold、原交付记录和保障时间不被覆盖 |
| A16 | 已交付全额退货/退款 | 验收原宠后 off；退款成功仍须复检才能上架 |
| A17 | 换宠锁定期间他人购买；换宠改退款 | 替换宠只能被一个业务占用；转退款释放替换占用并废止确认 |
| A18 | 在保障期末时刻/超期提交 | 按服务端时间及约定边界判断；不能改客户端时间延长期限 |
| A19 | 客户端上传 HTML伪图片、他人诊断报告或超大文件 | 拒绝非法格式/越权/超限；私有材料无永久公开URL |
| A20 | 修改已发布协议/商品价格 | 历史订单的价格、文本、hash、签署时间、媒体快照不变 |
| A21 | SSE断流、重试、超时、无资料问题 | 见10.6；保存唯一消息结果，明确失败和联系店主 |
| A22 | AI询问他人订单、行情价格、异宠、健康症状 | 身份校验、价格页引导、猫狗范围、就医提示均生效 |
| A23 | 导入半批非法行、索引服务失败、旧向量仍在 | 见11.6；结果可查、可恢复；不引用无效版本/已售商品 |
| A24 | 重复埋点、用户伪造支付事件、结束后点踩 | 去重；支付由后端事实产生；已结束反馈拒绝，快照不改变 |
| A25 | 后台修改时提交旧 version | 409 VERSION_CONFLICT，不覆盖其他已保存操作 |

### 13.3 性能验收边界

商品列表接口 P95≤500ms、页面首屏≤2s、AI首字渲染≤1.5s、完整答案≤3s均按 PRD 作为目标；不作为接口每次必达保证。记录样本量、设备/网络条件、并发、回答长度后再验收。AI 超时与兜底按10.4执行，30秒为失败上限，不代表放宽3秒体验目标。

支付通道、短信、OSS、DeepSeek、向量索引以及后台定时补偿均需在真实可用配置下联调；文档示例/Mock通过不代表商户资质、公众号网页授权或线上支付权限已可用。

## 14. 开发顺序、内部任务与待确认事项

### 14.1 建议开发顺序

| 阶段 | 内容 | 完成标准 |
|---|---|---|
| 1 | 全局响应/鉴权/文件；门店、协议、商品后台和公开展示 | 能录入一只含证明的宠物并上架；公开端无隐私泄露 |
| 2 | 结算、唯一占用、订单详情、取消和关单任务 | A03～A07通过，数据库状态一致 |
| 3 | 微信 JSAPI/H5、回调与查单、退款补偿 | 沙箱/测试配置跑通后再真实小额闭环；A08～A10、A13～A14通过 |
| 4 | 买家现场确认、核销、超期、售后、退回 | A11～A18通过；换宠须规则确认后启用 |
| 5 | 知识条目、导入、索引、AI SSE与身份约束 | 10.6、11.6、A21～A23通过 |
| 6 | 仪表盘、埋点、权限/并发/移动端联调 | 指标按既定口径采集；全部适用验收通过 |

以上阶段可先用 Mock 并行开发前端。接口数据真实化后，后端继续保留 Service 层的金额、权限、状态约束，不能因为前端隐藏按钮就省略校验。

### 14.2 必要的后端内部任务（不提供公开触发接口）

| 任务 | 默认频率/触发 | 必须满足 |
|---|---|---|
| 付款超时关单 | 每分钟扫描及支付入口实时检查 | 截止时间来自订单快照；查询/关闭微信交易后再释放 |
| 自提超期退款 | 每分钟扫描，交付入口同时检查截止 | paid 订单条件抢占；复用退款/售后意图，避免重复退 |
| 支付与退款对账补偿 | 周期扫描非终态，默认1分钟；持续异常进待办 | 稳定业务单号；未知结果不乱重试；实际金额核对 |
| 商品与知识索引同步 | 数据提交后可靠事件/任务 | 失败可重试，幂等；聊天推荐实时查业务库 |
| 检疫有效性检查 | 商品查询/下单/交付时检查；周期标记待复核 | 无有效证明不得成交/交付；预订订单保持占用并提示店主处理 |
| AI 超时/会话结束 | 30秒生成超时、30分钟无活动关会话 | 无永久 streaming 占位；统计快照只固化一次 |
| 临时上传清理 | 每日，仅未被资源引用且超24小时 | 不清理订单、诊断、协议证据、任务引用文件 |

证据实现至少需要独立或等效的数据结构：订单价格/宠物/门店/协议快照、支付单、退款单、售后单、退回记录、换宠关联、现场确认与操作审计、文件用途/归属、知识版本/任务、会话与事件。它们是接口必需的数据，不要求拆成微服务。

### 14.3 开发/上线前待确认清单

| 事项 | 当前建议/影响 |
|---|---|
| 售后库存冲突修正 | 采用第3章区分未交付退款与已交付退回的规则；原PRD尚需同步确认 |
| 订单 completed/picked_up | 采用一次交付完成，仅保留 pickedUpAt 事件字段 |
| 付款、自提、保障期限 | 暂按30分钟/72小时/168小时；确认按自然小时、边界包含和报告补交规则 |
| 同价换宠及后续保障 | 差价、换宠后保障期、新宠再申诉尚未定义；exchangeEnabled 默认 false，确认后冻结相应契约再启用 |
| 治疗费分担 | 暂按原订单部分退款，上限不超已付余额；超额补偿的线下处理不伪造微信退款 |
| 现场确认方式与联系人 | 暂用登录手机号本人短信确认；代提与手写签名后置 |
| 协议最终版本 | 经营者完成条款确认后发布；本文未验证法律适用性或替代正式协议 |
| 微信授权和支付资质 | 核验公众号网页授权、JSAPI绑定、H5支付开通、活体类目准入与域名配置 |
| 容量与留存 | 日UV/订单量、文件及聊天留存期限、成本上限仍待确认 |

业务规则确认或变化时，应同时更新 PRD 与本接口文档，并以接口版本变更记录告知前后端开发人员。
